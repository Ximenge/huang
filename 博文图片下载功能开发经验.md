# 博文图片打包下载功能 - 开发经验文档

## 一、需求背景

用户需求：在普通博文页面增加一个下载按钮，可以把博文里发布的图片，打包成一个与博文标题同名的 ZIP 压缩文件下载。

关键约束：
- 网站部署在 Cloudflare Pages 上，无持久化存储
- 图片存储在 Cloudflare R2（`image.91tutu.cc`），与网站域名（`91tutu.cc`）不同源
- 不能影响其他正常功能
- 只增加或修改必要的代码

---

## 二、设计思路与方案选型

### 方案对比

| 方案 | 可行性 | 优缺点 |
|------|--------|--------|
| 服务端 API 打包 | ❌ | Cloudflare Pages 无持久化存储，无法临时存大文件；Worker 有 CPU 时间限制 |
| 客户端 fetch 直接下载 | ⚠️ | 受 CORS 限制，跨域 fetch 可能失败（实测确实失败） |
| 客户端 Canvas 提取 | ⚠️ | 跨域图片会"污染"Canvas，`toBlob()` 抛出安全异常；需 `<img crossorigin="anonymous">` + CORS 头 |
| **客户端 fetch + 同源代理** | ✅ | 通过 Cloudflare Pages Function 代理图片请求，绕过 CORS；客户端 JSZip 打包 |

### 最终方案：客户端 JSZip + 同源代理 API

```
浏览器 → fetch(/api/proxy-image?url=xxx) → Cloudflare Pages Function → fetch(R2 图片) → 透传给浏览器
                                                                                                    ↓
浏览器 ← ZIP blob ← JSZip 打包所有图片 blob ← 并发下载代理 URL
```

**为什么选这个方案：**
1. 纯客户端打包，无需服务端存储
2. 同源代理绕过 CORS，100% 可靠
3. Cloudflare Pages Function 免费额度充足（10万次/天）
4. 代理可设置缓存头，减少重复请求

---

## 三、实现过程与遇到的问题

### 第一版：Canvas 方法（失败）

**思路**：浏览器渲染博文时已将图片下载到内存，通过 Canvas API 的 `drawImage` + `toBlob()` 提取像素数据。

**代码**：
```javascript
var canvas = document.createElement('canvas');
var ctx = canvas.getContext('2d');
canvas.width = img.naturalWidth;
canvas.height = img.naturalHeight;
ctx.drawImage(img, 0, 0, canvas.width, canvas.height);
canvas.toBlob(function(blob) {
  zip.file(fileName, blob);
}, 'image/webp', 0.95);
```

**问题**：跨域图片没有 `crossorigin="anonymous"` 属性，Canvas 被"污染"（tainted），`toBlob()` 抛出 `SecurityError`。博文中的图片是 Markdown 渲染的 `<img>` 标签，无法自动添加 `crossorigin` 属性。

**经验教训**：
- Canvas 方法只适用于同源图片或已设置 `crossorigin="anonymous"` 且服务端返回 CORS 头的图片
- Markdown 渲染的 `<img>` 标签无法控制其属性，Canvas 方法不可行

### 第二版：fetch 直接下载（失败）

**思路**：R2 配置了 CORS 头（`Access-Control-Allow-Origin: https://91tutu.cc`），直接用 `fetch({ mode: 'cors' })` 下载图片。

**代码**：
```javascript
fetch(url, { mode: 'cors' })
  .then(function(response) { return response.blob(); })
  .then(function(blob) { zip.file(fileName, blob); });
```

**问题**：所有图片返回 `net::ERR_FAILED`。

**排查过程**：
1. 用 `curl -H "Origin: https://91tutu.cc"` 测试，R2 确实返回了 `Access-Control-Allow-Origin: https://91tutu.cc`
2. 但浏览器行为和 curl 不同——`<img>` 标签不发送 `Origin` 头所以能正常显示，`fetch()` 发送 `Origin` 头触发 CORS 预检

**根本原因**：
- `<img>` 标签加载图片：浏览器不发 `Origin` 头，不触发 CORS 检查，图片正常显示
- `fetch({ mode: 'cors' })`：浏览器发送 `Origin` 头，触发 CORS 预检请求（OPTIONS），服务端可能不正确处理预检请求，导致 CORS 失败

**经验教训**：
- **curl 测试 CORS 不可靠**：curl 能看到 CORS 头不代表浏览器就能通过 CORS 检查
- **`<img>` 能加载 ≠ `fetch` 能获取**：这是最常见的 CORS 误解
- **跨域资源下载必须考虑 CORS**：即使资源能正常显示，fetch 仍可能失败

### 第三版：同源代理 API（成功）

**思路**：创建 Cloudflare Pages Function 作为同源代理，服务端 fetch R2 图片后透传给浏览器。

**代码**：
```typescript
// src/pages/api/proxy-image.ts
import type { APIRoute } from 'astro';

export const prerender = false;

export const GET: APIRoute = async ({ url }) => {
  const imageUrl = url.searchParams.get('url');

  if (!imageUrl) {
    return new Response('Missing url parameter', { status: 400 });
  }

  // 安全校验：仅允许代理指定域名
  if (!imageUrl.startsWith('https://image.91tutu.cc/')) {
    return new Response('Forbidden', { status: 403 });
  }

  try {
    const response = await fetch(imageUrl, {
      headers: { 'User-Agent': '91tutu-ImageProxy/1.0' },
    });

    if (!response.ok) {
      return new Response('Upstream error', { status: response.status });
    }

    const contentType = response.headers.get('Content-Type') || 'image/webp';

    return new Response(response.body, {
      status: 200,
      headers: {
        'Content-Type': contentType,
        'Access-Control-Allow-Origin': '*',
        'Cache-Control': 'public, max-age=604800',
      },
    });
  } catch (error) {
    return new Response('Fetch failed', { status: 502 });
  }
};
```

**客户端调用**：
```javascript
function proxyUrl(originalUrl) {
  return '/api/proxy-image?url=' + encodeURIComponent(originalUrl);
}

fetch(proxyUrl(originalUrl))
  .then(function(response) { return response.blob(); })
  .then(function(blob) { zip.file(decodedName, blob); });
```

**为什么成功**：
- 请求走同源路径 `/api/proxy-image`，浏览器不触发 CORS 检查
- 服务端 fetch 无 CORS 限制，可直接获取 R2 图片
- 代理 API 添加了 `Access-Control-Allow-Origin: *`，即使有 CORS 检查也能通过

**经验教训**：
- **同源代理是解决 CORS 最可靠的方案**：服务端不受浏览器 CORS 限制
- **代理 API 必须做安全校验**：限制允许代理的域名，防止被滥用为开放代理
- **添加缓存头**：`Cache-Control: public, max-age=604800`，减少重复请求，提升性能

### 问题四：客户端路由后下载按钮失效

**现象**：用户通过站内导航跳转到另一个博文页面后，下载按钮点击无反应。

**原因**：Astro 开启了 `prefetch`，站内导航是客户端路由（不整页刷新）。`script is:inline` 的 IIFE 只在首次加载执行一次，闭包中的 `zipFileName` 仍是旧页面的标题。

**修复**：
1. `getZipFileName()` 改为每次点击时实时从 DOM 读取，而非初始化时闭包捕获
2. 监听 `astro:page-load` 事件重新绑定按钮点击事件

```javascript
// 修复前：闭包捕获（过期）
var zipFileName = (function() {
  var el = document.getElementById('download-post-title');
  return el ? el.textContent.trim() : 'images';
})();

// 修复后：实时读取
function getZipFileName() {
  var el = document.getElementById('download-post-title');
  var name = el ? el.textContent.trim() : 'images';
  return name.replace(/[\/\\:*?"<>|]/g, '_');
}

// 客户端路由兼容
document.addEventListener('DOMContentLoaded', initDownloadButton);
document.addEventListener('astro:page-load', initDownloadButton);
```

**经验教训**：
- **Astro 客户端路由下，`script is:inline` 的 IIFE 只执行一次**
- **不要在 IIFE 初始化时捕获 DOM 值**，应在事件触发时实时读取
- **必须监听 `astro:page-load` 事件**来处理客户端路由后的 DOM 更新

### 问题五：ZIP 内图片被重命名

**现象**：ZIP 内文件名为 `001.webp`、`002.webp`，而非原始文件名。

**原因**：代码中使用了序号命名：
```javascript
var fileName = String(currentIndex + 1).padStart(3, '0') + ext;
zip.file(fileName, blob);
```

**修复**：直接使用 URL 中的原始文件名：
```javascript
var urlParts = originalUrl.split('/');
var originalName = urlParts[urlParts.length - 1] || 'image';
var decodedName = decodeURIComponent(originalName);
zip.file(decodedName, blob);
```

**经验教训**：
- **保留原始文件名更符合用户预期**
- **URL 中的文件名需要 `decodeURIComponent` 解码**，否则中文会显示为编码形式

---

## 四、涉及文件与代码

### 文件清单

| 文件 | 路径 | 操作 | 说明 |
|------|------|------|------|
| proxy-image.ts | `src/pages/api/proxy-image.ts` | 新增 | 图片代理 API |
| jszip.min.js | `public/jszip.min.js` | 新增 | JSZip 库（从 node_modules 复制） |
| DownloadAllImages.astro | `src/components/DownloadAllImages.astro` | 重写 | 下载按钮组件 |
| [...slug].astro | `src/pages/posts/[...slug].astro` | 修改 | 博文页面模板 |

### 各文件职责

#### proxy-image.ts — 图片代理 API

- Cloudflare Pages Function，`prerender: false` 确保服务端渲染
- 接收 `?url=` 参数，校验域名白名单后代理获取图片
- 透传原始 `Content-Type`，添加缓存头

#### jszip.min.js — JSZip 库

- 从 `node_modules/jszip/dist/jszip.min.js` 复制到 `public/`
- 按需加载：首次点击下载按钮时才加载（~97KB）
- 不影响页面初始加载性能

#### DownloadAllImages.astro — 下载组件

- 接收 `postTitle` 属性
- 收集 `article` 内所有有效图片 URL
- 过滤规则：排除 `data:image`、`pagefind`、`close-icon`、`afcdn.net`、尺寸 < 50px
- 并发控制：5 个同时下载
- 通过代理 URL 下载图片
- JSZip 打包，DEFLATE 压缩
- 保留原始文件名

#### [...slug].astro — 博文页面

- 导入 DownloadAllImages 组件
- 添加隐藏 `<span id="download-post-title">` 传递标题给 JS
- 在 `<Content />` 前添加下载按钮

---

## 五、可复用开发经验

### 经验 1：跨域资源下载的可靠方案

**场景**：需要在前端下载跨域资源（图片、文件等）

**方案优先级**：
1. **同源代理 API**（最可靠）— 服务端 fetch 无 CORS 限制
2. Canvas 提取（需 `crossorigin="anonymous"` + CORS 头）
3. fetch 直接下载（需服务端正确配置 CORS）

**代理 API 模板**：
```typescript
export const prerender = false;

export const GET: APIRoute = async ({ url }) => {
  const targetUrl = url.searchParams.get('url');
  if (!targetUrl) return new Response('Missing url', { status: 400 });
  if (!targetUrl.startsWith('https://allowed-domain.com/')) return new Response('Forbidden', { status: 403 });

  const response = await fetch(targetUrl);
  return new Response(response.body, {
    headers: {
      'Content-Type': response.headers.get('Content-Type') || 'application/octet-stream',
      'Access-Control-Allow-Origin': '*',
      'Cache-Control': 'public, max-age=604800',
    },
  });
};
```

### 经验 2：Astro 客户端路由下的 JS 注意事项

**场景**：Astro 开启 `prefetch` 后，站内导航是客户端路由

**注意事项**：
- `script is:inline` 的 IIFE 只在首次加载执行一次
- 不要在初始化时闭包捕获 DOM 值，应在事件触发时实时读取
- 必须监听 `astro:page-load` 事件处理路由后的 DOM 更新
- 使用 `removeEventListener` + `addEventListener` 避免重复绑定

### 经验 3：Cloudflare Pages Function 使用要点

- 必须设置 `export const prerender = false`，否则会被静态构建
- 支持的运行时 API：`fetch`、`Request`、`Response`、`URL`
- 可访问 Cloudflare Bindings（KV、D1、R2 等）通过 `context.locals.runtime.env`
- 免费额度：10万次请求/天，足够个人站点使用

### 经验 4：JSZip 按需加载

- 将 `jszip.min.js` 放在 `public/` 目录，不打包进主 JS
- 首次使用时动态创建 `<script>` 标签加载
- 加载后 `typeof JSZip !== 'undefined'` 检查是否可用
- 避免影响页面初始加载性能

### 经验 5：图片过滤策略

博文页面可能包含非内容图片（广告、图标等），需要过滤：

```javascript
// 过滤规则
img.src &&
!img.src.includes('data:image') &&      // 排除 base64 图片
!img.src.includes('pagefind') &&         // 排除搜索组件图标
!img.src.includes('close-icon') &&       // 排除关闭按钮图标
!img.src.includes('afcdn.net') &&        // 排除广告域名
img.naturalWidth > 50 &&                 // 排除小图标
img.naturalHeight > 50                   // 排除小图标
```

---

## 六、Cloudflare Pages 部署限制总结

| 限制 | 说明 | 应对策略 |
|------|------|----------|
| 无持久化存储 | 不能临时存放大文件 | 客户端打包 |
| Worker CPU 时间限制 | 免费版 10ms/请求 | 代理 API 只做透传，不处理数据 |
| 请求大小限制 | 免费版 100MB | 图片逐个代理，不一次性获取 |
| CORS | 浏览器跨域限制 | 同源代理 API |
| 客户端路由 | Astro prefetch 不整页刷新 | 监听 `astro:page-load` 事件 |
