# 弹窗广告使用说明

## 1. 当前实现方式

项目有两种弹窗广告，使用不同的实现方式：

| 弹窗类型 | 组件 | 实现方式 | 触发方式 | 开关 |
|---------|------|---------|---------|------|
| 全站弹窗 | `ExoClickPopunder.astro` | popMagic V7 内联代码 | 页面任意点击 | `POPUNDER_ENABLED` |
| 下载弹窗 | `DownloadPopunder.astro` | 直接重定向 URL | 仅下载按钮 | `DOWNLOAD_POPUNDER_ENABLED` |

### 配置位置

所有参数在 `src/consts.ts` 中集中管理：

```typescript
// 全站弹窗（popMagic V7 内联代码）
export const POPUNDER_ENABLED = false;
export const POPUNDER_IDZONE = "5866630";
export const POPUNDER_FREQUENCY_PERIOD = 60;
export const POPUNDER_FREQUENCY_COUNT = 1;
export const POPUNDER_TRIGGER_METHOD = 1;

// 下载弹窗（直接重定向 URL）
export const DOWNLOAD_POPUNDER_ENABLED = true;
export const DOWNLOAD_POPUNDER_IDZONE = "5914280";
```

## 2. 下载弹窗（直接重定向方式）

### 工作原理

点击下载按钮时，直接在新标签页打开广告重定向 URL：

```
https://s.pemsrv.com/v1/link.php?cat=&idzone=5914280&type=8&p={当前页面URL}&cb={随机数}
```

- **每次点击都会弹窗**，频率由广告商控制
- 不使用 Cookie 控制频率，不依赖 popMagic 脚本
- `p` 参数：告诉广告商点击来源页面（非必须，有助于统计）
- `cb` 参数：防止浏览器缓存，确保每次点击算独立请求（非必须，有助于计数准确）

### 配置参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `DOWNLOAD_POPUNDER_ENABLED` | Boolean | true | 开关 |
| `DOWNLOAD_POPUNDER_IDZONE` | String | "5914280" | 广告位 ID |

### 如何控制

| 操作 | 修改方式 |
|------|---------|
| 开启下载弹窗 | `DOWNLOAD_POPUNDER_ENABLED = true` |
| 关闭下载弹窗 | `DOWNLOAD_POPUNDER_ENABLED = false` |
| 更换广告位 | 修改 `DOWNLOAD_POPUNDER_IDZONE` |

## 3. 全站弹窗（popMagic V7 内联代码）

### 工作原理

1. **脚本加载**：页面加载时执行内联 V7 popMagic 代码
2. **配置读取**：从 `define:vars` 注入的变量读取配置参数
3. **事件监听**：根据 `trigger_method` 设置点击事件监听
4. **频率控制**：使用 Cookie (`zone-cap-{idzone}`) 记录用户已看到的广告次数
5. **弹窗触发**：
   - 用户点击时，在新标签页或当前页打开广告链接
   - 广告链接由 `buildUrl()` 生成：`s.pemsrv.com/v1/link.php?idzone=...&type=8&...`
6. **浏览器适配**：针对 Chrome 使用特殊处理方式

### 配置参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `POPUNDER_ENABLED` | Boolean | false | 开关 |
| `POPUNDER_IDZONE` | String | "5866630" | 广告位 ID |
| `POPUNDER_FREQUENCY_PERIOD` | Number | 60 | 频率限制周期（分钟） |
| `POPUNDER_FREQUENCY_COUNT` | Number | 1 | 周期内最多显示次数 |
| `POPUNDER_TRIGGER_METHOD` | Number | 1 | 触发方式 |

### 触发方式 (trigger_method)

| 值 | 触发方式 | 说明 |
|----|----------|------|
| 1 | 任意点击 | 点击页面任意位置触发 |
| 2 | 特定class | 点击带有指定class的元素触发 |
| 3 | 点击链接 | 点击链接时触发 |
| 4 | 非特定class | 点击非指定class的元素触发 |
| 5 | 非链接 | 点击非链接元素时触发 |

### 如何控制

| 操作 | 修改方式 |
|------|---------|
| 开启全站弹窗 | `POPUNDER_ENABLED = true` |
| 关闭全站弹窗 | `POPUNDER_ENABLED = false` |
| 每次点击都触发 | `POPUNDER_FREQUENCY_PERIOD = 0` 且 `POPUNDER_FREQUENCY_COUNT = 9999` |
| 每30分钟触发1次 | `POPUNDER_FREQUENCY_PERIOD = 30`，`POPUNDER_FREQUENCY_COUNT = 1` |
| 更换广告位 | 修改 `POPUNDER_IDZONE` |

## 4. 两种实现方式的区别

| 对比项 | 全站弹窗 | 下载弹窗 |
|-------|---------|---------|
| 实现方式 | popMagic V7 内联代码 | 直接重定向 URL |
| 代码量 | ~300行压缩代码 | ~20行 |
| 触发方式 | 页面任意点击 | 仅下载按钮 |
| 频率控制 | Cookie 控制（本地） | 广告商控制（服务端） |
| idzone | 5866630 | 5914280 |
| 浏览器适配 | 自动适配 Chrome/Firefox/Safari | 依赖浏览器 `window.open()` |
| 依赖 | popMagic 内联脚本 | 无外部依赖 |

## 5. 技术要点

### 为什么下载弹窗不用 popMagic

之前下载弹窗也用 popMagic V7 内联代码，但存在两个问题：
1. **Cookie 频率限制**：popMagic 用 Cookie 控制频率，但广告商服务端也有自己的频率限制，两边限制叠加导致"只弹一次"
2. **代码复杂**：popMagic 代码几百行，而下载弹窗只需要"点击按钮→打开广告URL"，不需要那么复杂的逻辑

改为直接重定向方式后，每次点击都打开广告 URL，频率完全由广告商控制，简单可靠。

### 为什么全站弹窗还用 popMagic

全站弹窗需要"页面任意点击触发"+"浏览器适配（Chrome 特殊处理）"+"Cookie 频率控制"，这些功能 popMagic 已经封装好了，直接使用更方便。

### 为什么 only_inline: true（全站弹窗）

- `only_inline: false` 时会加载外部 V8 脚本，覆盖 V7 代码
- V8 覆盖后 `buildUrl()` 生成错误URL → 404
- `only_inline: true` 阻止加载外部脚本，V7 独立运行

### 为什么 tcf_enabled: false（全站弹窗）

网站没有 GDPR 同意框架（`__tcfapi`），开启会导致 `popMagic.browser` 为 null 报错。

## 6. 注意事项

1. **广告位 ID**：下载弹窗 `5914280`、全站弹窗 `5866630`，不要随意修改
2. **频率限制**：下载弹窗频率由广告商控制；全站弹窗频率由 Cookie 控制
3. **调试**：清除 Cookie（`zone-cap_*`）可重置全站弹窗的频率限制
4. **浏览器弹窗拦截**：Chrome 可能拦截 `window.open()` 弹窗，用户需允许弹窗

## 7. 修改后操作

修改 `consts.ts` 后，需要重新构建并部署：

```bash
npm run build
npx wrangler pages deploy dist --project-name=astro-melody-starter --branch=main
```
