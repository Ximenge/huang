# 更新日志

## 格式要求

每次更新日志必须包含以下内容：
1. **功能** - 本次更新实现了什么功能
2. **实现逻辑** - 功能的技术实现原理
3. **重要参数** - 需要记住的关键配置参数
4. **代码变更** - 修改了哪些代码，增加和删减了哪些文件
5. **修改文件清单** - 修改的代码的文件名和相对路径

### 格式规范
- 标题格式：`## 版本号 - 更新内容简述 (日期 时间)`
- 时间精确到秒
- 每次更新版本号递增（主版本号.次版本号.修订号）
- 每次更新往后续写，不删除历史记录
- 相对路径以 `huang` 目录为根目录

---

## v1.0.0 - 集成 ExoClick Popunder 弹窗广告 (2026-03-07 02:51:00)

### 功能

1. **ExoClick Popunder 弹窗广告集成**
   - 在网站所有页面集成 ExoClick 官方 Popunder 广告代码
   - 实现点击触发弹窗广告功能
   - 支持频率控制，避免过度打扰用户
   - 爬虫不显示广告，不影响 SEO

2. **创建简化版 Popunder 脚本**
   - 作为备用方案，提供自定义 Popunder 实现
   - 支持多种触发方式和配置参数

### 实现逻辑

#### Popunder 广告工作原理

1. **脚本加载**：页面加载时异步加载 ExoClick 官方代码
2. **配置读取**：从 `adConfig` 对象读取广告配置参数
3. **事件监听**：根据 `trigger_method` 设置点击事件监听
   - `trigger_method: 1` = 点击页面任意位置触发
   - `trigger_method: 2` = 点击特定 class 元素触发
   - `trigger_method: 3` = 点击链接时触发
4. **频率控制**：使用 Cookie 记录用户已看到的广告次数
   - Cookie 名称格式：`zone-cap-{idzone}`
   - 存储格式：`次数;时间戳`
5. **弹窗触发**：
   - 用户点击时，在新标签页打开当前页面链接
   - 在当前页面重定向到广告链接
6. **浏览器适配**：
   - Chrome：使用 `Ctrl+点击` 模拟方式
   - 移动端：使用兼容的弹窗方式
   - 其他浏览器：标准方式

#### 爬虫检测逻辑

```javascript
const botUserAgents = [
  'googlebot', 'bingbot', 'slurp', 'duckduckbot', 
  'baiduspider', 'yandexbot', 'facebookexternalhit',
  'twitterbot', 'linkedinbot', 'whatsapp', 'telegrambot',
  'applebot', 'petalbot', 'ahrefsbot', 'semrushbot',
  'mj12bot', 'dotbot', 'rogerbot', 'screaming frog'
];
const isBot = botUserAgents.some(bot => userAgent.toLowerCase().includes(bot));
```

### 重要参数

#### ExoClick 广告配置 (来自 pp.md)

| 参数 | 值 | 说明 |
|------|-----|------|
| `ads_host` | `a.pemsrv.com` | 广告主机 |
| `syndication_host` | `s.pemsrv.com` | 同步主机 |
| `idzone` | `5866630` | 广告位 ID |
| `frequency_period` | `60` | 频率限制周期（分钟） |
| `frequency_count` | `2` | 周期内最多显示次数 |
| `trigger_method` | `1` | 触发方式（1=任意点击） |
| `trigger_delay` | `0` | 触发延迟（秒） |
| `chrome_enabled` | `true` | Chrome 浏览器启用 |
| `new_tab` | `false` | 是否在新标签页打开 |
| `popup_fallback` | `false` | 弹窗回退 |
| `popup_force` | `false` | 强制弹窗 |
| `capping_enabled` | `true` | 启用频率限制 |
| `tcf_enabled` | `true` | TCF 同意框架 |
| `only_inline` | `false` | 仅内联 |

#### 触发方式说明

| 值 | 触发方式 | 适用场景 |
|----|----------|----------|
| 1 | 任意点击 | 高收益，用户体验较差 |
| 2 | 特定 class | 精准控制，需要指定元素 |
| 3 | 点击链接 | 平衡收益和体验（推荐） |
| 4 | 非特定 class | 排除特定元素 |
| 5 | 非链接 | 点击非链接区域 |

### 代码变更

#### 新增文件

| 文件路径 | 说明 |
|----------|------|
| `astro-melody-starter/src/components/ExoClickPopunder.astro` | ExoClick 官方 Popunder 广告组件 |
| `astro-melody-starter/public/popunder.js` | 简化版 Popunder 脚本（备用） |
| `popunder-usage.md` | Popunder 广告使用说明文档 |

#### 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `astro-melody-starter/src/layouts/BaseLayout.astro` | 添加 ExoClickPopunder 组件导入和使用 |

#### 修改详情

**BaseLayout.astro 修改：**

```diff
---
import BaseHead from "@components/BaseHead.astro";
import Header from "@components/Header.astro";
import Footer from "@components/Footer.astro";
import BottomAd from "@components/BottomAd.astro";
+ import ExoClickPopunder from "@components/ExoClickPopunder.astro";
const { title, description } = Astro.props;
import "../assets/app.css";
---

...

  <body
    class="flex flex-col min-h-screen mx-auto max-w-6xl px-4 dark:prose-invert sm:px-6 lg:px-8"
  >
+   <!-- ExoClick Popunder 广告 - 爬虫不显示 -->
+   {!isBot && <ExoClickPopunder />}
+   
    <Header />
```

#### 删除文件

无

### 修改文件清单

| 文件名 | 相对路径 | 操作类型 |
|--------|----------|----------|
| ExoClickPopunder.astro | `astro-melody-starter/src/components/ExoClickPopunder.astro` | 新增 |
| popunder.js | `astro-melody-starter/public/popunder.js` | 新增 |
| popunder-usage.md | `popunder-usage.md` | 新增 |
| BaseLayout.astro | `astro-melody-starter/src/layouts/BaseLayout.astro` | 修改 |

### 部署信息

- **部署时间**：2026-03-07 02:51:00
- **部署地址**：https://91tutu.cc

---

## v1.0.7 - 博文页面新增图片打包下载功能 (2026-04-24 20:05:00)

### 功能

1. **博文图片打包下载**
   - 在博文详情页顶部显示"下载 ZIP"按钮
   - 点击按钮将博文内所有图片打包成 ZIP 文件下载
   - ZIP 文件名与博文标题同名（自动去除非法字符）
   - ZIP 内图片保留原始文件名和格式
   - 支持并发下载（5个同时），带实时进度显示

2. **同源代理 API**
   - 新增 `/api/proxy-image` 代理接口，解决跨域图片下载问题
   - 仅允许代理 `https://image.91tutu.cc/` 域名的图片
   - 服务端缓存 7 天（`Cache-Control: public, max-age=604800`）

3. **客户端路由兼容**
   - 下载按钮在 Astro 客户端路由跳转后仍然可用
   - 每次点击时实时读取当前页面标题，不依赖初始化时闭包

### 实现逻辑

#### 整体架构

```
用户点击"下载 ZIP"
    ↓
收集 article 内所有 <img> 的 src（过滤广告/图标）
    ↓
按需加载 JSZip 库（/jszip.min.js）
    ↓
并发 fetch 代理 URL（/api/proxy-image?url=xxx）
    ↓
代理 API 服务端获取 R2 图片 → 透传给浏览器
    ↓
JSZip 打包所有图片 blob → 生成 ZIP
    ↓
触发浏览器下载「博文标题.zip」
```

#### 为什么需要代理 API

浏览器 `fetch()` 跨域请求 R2 图片时触发 CORS 检查失败（`net::ERR_FAILED`）。虽然 R2 配置了 CORS 头，但 `<img>` 标签加载和 `fetch()` 行为不同：
- `<img>` 不发送 `Origin` 头，浏览器允许加载
- `fetch({ mode: 'cors' })` 发送 `Origin` 头，触发 CORS 预检，服务端返回的 CORS 头可能不匹配

解决方案：通过同源代理 API（Cloudflare Pages Function）在服务端获取图片，绕过浏览器 CORS 限制。

#### 客户端路由兼容

Astro 开启了 `prefetch`，用户通过站内导航跳转博文时是客户端路由（不整页刷新）。`script is:inline` 的 IIFE 只在首次加载执行一次，因此：
- `getZipFileName()` 每次点击时实时从 DOM 读取标题，而非初始化时闭包捕获
- 监听 `astro:page-load` 事件重新绑定按钮点击事件

### 重要参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `CONCURRENCY` | 5 | 并发下载数 |
| `Cache-Control` | `public, max-age=604800` | 代理缓存 7 天 |
| `compression` | `DEFLATE` | ZIP 压缩算法 |
| `compressionOptions.level` | 6 | ZIP 压缩级别 |
| JSZip 文件大小 | ~97KB | 按需加载 |

### 代码变更

#### 新增文件

| 文件路径 | 说明 |
|----------|------|
| `astro-melody-starter/src/pages/api/proxy-image.ts` | 图片代理 API（Cloudflare Pages Function） |
| `astro-melody-starter/public/jszip.min.js` | JSZip 库（从 node_modules 复制） |

#### 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `astro-melody-starter/src/components/DownloadAllImages.astro` | 重写下载组件，使用代理 API + 保留原始文件名 |
| `astro-melody-starter/src/pages/posts/[...slug].astro` | 导入 DownloadAllImages 组件，添加下载按钮和隐藏标题元素 |

#### 修改详情

**proxy-image.ts 新增：**

```typescript
import type { APIRoute } from 'astro';

export const prerender = false;

export const GET: APIRoute = async ({ url }) => {
  const imageUrl = url.searchParams.get('url');

  if (!imageUrl) {
    return new Response('Missing url parameter', { status: 400 });
  }

  if (!imageUrl.startsWith('https://image.91tutu.cc/')) {
    return new Response('Forbidden', { status: 403 });
  }

  try {
    const response = await fetch(imageUrl, {
      headers: {
        'User-Agent': '91tutu-ImageProxy/1.0',
      },
    });

    if (!response.ok) {
      return new Response('Upstream error', { status: response.status });
    }

    const contentType = response.headers.get('Content-Type') || 'image/webp';

    return new Response(response.body, {
      status: 200,
      headers: {
        'Content-Type': contentType,
        'Access-Control-Allow-Origin': '*',
        'Cache-Control': 'public, max-age=604800',
      },
    });
  } catch (error) {
    return new Response('Fetch failed', { status: 502 });
  }
};
```

**[...slug].astro 修改：**

```diff
---
import { getCollection, render } from "astro:content";
import BaseLayout from "@layouts/BaseLayout.astro";
import RelatedPostsCard from "@components/RelatedPostsCard.astro";
import FormattedDate from "@components/FormattedDate.astro";
+ import DownloadAllImages from "@components/DownloadAllImages.astro";
---

      <div>
+       <span id="download-post-title" class="hidden">{post.data.title}</span>
+       <DownloadAllImages postTitle={post.data.title} />
        <Content />
      </div>
```

**DownloadAllImages.astro 关键逻辑：**

```javascript
// 实时读取标题（非闭包捕获）
function getZipFileName() {
  var el = document.getElementById('download-post-title');
  var name = el ? el.textContent.trim() : 'images';
  return name.replace(/[\/\\:*?"<>|]/g, '_');
}

// 通过代理 URL 下载（绕过 CORS）
function proxyUrl(originalUrl) {
  return '/api/proxy-image?url=' + encodeURIComponent(originalUrl);
}

// 保留原始文件名
var urlParts = originalUrl.split('/');
var originalName = urlParts[urlParts.length - 1] || 'image';
var decodedName = decodeURIComponent(originalName);
zip.file(decodedName, blob);

// 客户端路由兼容
document.addEventListener('DOMContentLoaded', initDownloadButton);
document.addEventListener('astro:page-load', initDownloadButton);
```

#### 删除文件

无

### 修改文件清单

| 文件名 | 相对路径 | 操作类型 |
|--------|----------|----------|
| proxy-image.ts | `astro-melody-starter/src/pages/api/proxy-image.ts` | 新增 |
| jszip.min.js | `astro-melody-starter/public/jszip.min.js` | 新增 |
| DownloadAllImages.astro | `astro-melody-starter/src/components/DownloadAllImages.astro` | 修改 |
| [...slug].astro | `astro-melody-starter/src/pages/posts/[...slug].astro` | 修改 |

### 部署信息

- **部署时间**：2026-04-24 20:05:00
- **部署地址**：https://91tutu.cc
- **预览地址**：https://514b9ea2.astro-melody-starter.pages.dev

---

<!-- 后续更新请在此行下方添加 -->

## v1.0.1 - 调整弹窗广告频率为1小时1次 (2026-03-07 02:59:00)

### 功能

1. **调整 Popunder 弹窗广告频率**
   - 将弹窗频率从每60分钟2次调整为每60分钟1次
   - 减少用户打扰，提升用户体验

### 实现逻辑

修改 ExoClickPopunder.astro 组件中的 `adConfig` 配置：
- `frequency_period`: 60（保持不变，60分钟周期）
- `frequency_count`: 2 → 1（周期内最多显示次数从2次改为1次）

### 重要参数

#### 修改前后对比

| 参数 | 修改前 | 修改后 | 说明 |
|------|--------|--------|------|
| `frequency_period` | 60 | 60 | 频率限制周期（分钟）|
| `frequency_count` | 2 | 1 | 周期内最多显示次数 |

### 代码变更

#### 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `astro-melody-starter/src/components/ExoClickPopunder.astro` | 修改 `frequency_count` 从 2 改为 1 |

#### 修改详情

**ExoClickPopunder.astro 修改：**

```diff
  var adConfig = {
      "ads_host": "a.pemsrv.com",
      "syndication_host": "s.pemsrv.com",
      "idzone": 5866630,
      "popup_fallback": false,
      "popup_force": false,
      "chrome_enabled": true,
      "new_tab": false,
      "frequency_period": 60,
-     "frequency_count": 2,
+     "frequency_count": 1,
      "trigger_method": 1,
      "trigger_class": "",
      "trigger_delay": 0,
      "capping_enabled": true,
      "tcf_enabled": true,
      "only_inline": false
  };
```

#### 新增文件

无

#### 删除文件

无

### 修改文件清单

| 文件名 | 相对路径 | 操作类型 |
|--------|----------|----------|
| ExoClickPopunder.astro | `astro-melody-starter/src/components/ExoClickPopunder.astro` | 修改 |

### 部署信息

- **部署时间**：2026-03-07 02:59:00
- **部署地址**：https://91tutu.cc
- **预览地址**：https://596f525d.astro-melody-starter.pages.dev

---

## v1.0.2 - 调整留言板布局结构 (2026-03-07 03:04:00)

### 功能

1. **调整留言板页面布局**
   - 将"发表留言"表单移至页面顶部
   - 将"留言规则"移至表单下方
   - 将留言列表移至最底部
   - 移除左右分栏布局，改为单列布局

### 实现逻辑

修改 guestbook.astro 页面布局：
- 移除 `grid grid-cols-1 lg:grid-cols-3` 分栏布局
- 将发表留言表单从侧边栏移到主内容区顶部
- 将留言规则从侧边栏移到表单下方
- 将留言列表移到页面最底部
- 移除 `sticky top-4` 固定定位样式

### 重要参数

无新增参数

### 代码变更

#### 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `astro-melody-starter/src/pages/guestbook.astro` | 调整页面布局结构，移除分栏，改为单列布局 |

#### 修改详情

**guestbook.astro 布局调整：**

```diff
- <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
-   <div class="lg:col-span-2">
-     <div id="comments-list" class="space-y-4">
-       ...
-     </div>
-   </div>
-   <div class="lg:col-span-1">
-     <div class="sticky top-4">
-       <div class="bg-base-200 rounded-xl p-6 shadow-sm">
-         <h2>发表留言</h2>
-         ...
-       </div>
-       <div class="mt-4 bg-base-200 rounded-xl p-4 shadow-sm">
-         <h3>留言规则</h3>
-         ...
-       </div>
-     </div>
-   </div>
- </div>

+ <div class="bg-base-200 rounded-xl p-6 shadow-sm mb-6">
+   <h2>发表留言</h2>
+   ...
+ </div>
+ 
+ <div class="bg-base-200 rounded-xl p-4 shadow-sm mb-6">
+   <h3>留言规则</h3>
+   ...
+ </div>
+ 
+ <div id="comments-list" class="space-y-4">
+   ...
+ </div>
```

#### 新增文件

无

#### 删除文件

无

### 修改文件清单

| 文件名 | 相对路径 | 操作类型 |
|--------|----------|----------|
| guestbook.astro | `astro-melody-starter/src/pages/guestbook.astro` | 修改 |

### 部署信息

- **部署时间**：2026-03-07 03:04:00
- **部署地址**：https://91tutu.cc
- **预览地址**：https://77ebcfab.astro-melody-starter.pages.dev

---

## v1.0.3 - 留言板添加无限滚动分页功能 (2026-03-07 03:10:00)

### 功能

1. **留言板无限滚动分页**
   - 支持滚动到底部自动加载更多留言
   - 每页默认加载 10 条留言
   - 显示"加载更多..."和"没有更多留言了"状态提示
   - 提交留言后自动刷新并回到第一页

### 实现逻辑

#### 后端 API 分页

修改 `comments.ts` API：
- 支持 `?page=1&limit=10` 查询参数
- 返回分页信息：`{ comments, pagination: { page, limit, total, totalPages, hasMore } }`

#### 前端无限滚动

使用 `IntersectionObserver` API 实现无限滚动：
- 监听底部触发元素 `#load-more-trigger`
- 当元素进入视口时自动加载下一页
- 使用 `rootMargin: '100px'` 提前触发加载

### 重要参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `limit` | 10 | 每页留言数量 |
| `rootMargin` | 100px | 提前触发加载的距离 |

#### API 响应格式

```typescript
interface ApiResponse {
  comments: Comment[];
  pagination: {
    page: number;       // 当前页码
    limit: number;      // 每页数量
    total: number;      // 总留言数
    totalPages: number; // 总页数
    hasMore: boolean;   // 是否有更多
  };
}
```

### 代码变更

#### 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `astro-melody-starter/src/pages/api/comments.ts` | 添加分页参数支持 |
| `astro-melody-starter/src/pages/guestbook.astro` | 实现无限滚动加载 |

#### 修改详情

**comments.ts API 修改：**

```diff
- const comments = await GUESTBOOK.get('comments:all', 'json') as Comment[] || [];
- return new Response(JSON.stringify(comments), {

+ const url = new URL(context.request.url);
+ const page = parseInt(url.searchParams.get('page') || '1');
+ const limit = parseInt(url.searchParams.get('limit') || '10');
+ 
+ const allComments = await GUESTBOOK.get('comments:all', 'json') as Comment[] || [];
+ const total = allComments.length;
+ const totalPages = Math.ceil(total / limit);
+ 
+ const startIndex = (page - 1) * limit;
+ const endIndex = startIndex + limit;
+ const comments = allComments.slice(startIndex, endIndex);
+ 
+ return new Response(JSON.stringify({
+   comments,
+   pagination: {
+     page,
+     limit,
+     total,
+     totalPages,
+     hasMore: page < totalPages
+   }
+ }), {
```

**guestbook.astro 前端修改：**

```diff
+ let currentPage = 1;
+ let isLoading = false;
+ let hasMore = true;
+ const limit = 10;

+ function setupInfiniteScroll() {
+   const trigger = document.getElementById('load-more-trigger');
+   if (!trigger) return;
+   
+   const observer = new IntersectionObserver((entries) => {
+     entries.forEach(entry => {
+       if (entry.isIntersecting && hasMore && !isLoading) {
+         loadMoreComments();
+       }
+     });
+   }, {
+     rootMargin: '100px'
+   });
+   
+   observer.observe(trigger);
+ }
```

#### 新增文件

无

#### 删除文件

无

### 修改文件清单

| 文件名 | 相对路径 | 操作类型 |
|--------|----------|----------|
| comments.ts | `astro-melody-starter/src/pages/api/comments.ts` | 修改 |
| guestbook.astro | `astro-melody-starter/src/pages/guestbook.astro` | 修改 |

### 部署信息

- **部署时间**：2026-03-07 03:10:00
- **部署地址**：https://91tutu.cc
- **预览地址**：https://4e2bacc7.astro-melody-starter.pages.dev

---

## v1.0.4 - 移动端导航栏优化 (2026-03-07 03:16:00)

### 功能

1. **移动端导航栏优化**
   - 三个导航按钮（视频、标签、留言板）在手机屏幕上始终显示
   - 移除汉堡菜单，改为直接显示导航按钮
   - 响应式设计：小屏幕使用较小字体和间距

### 实现逻辑

修改 Header.astro 组件：
- 移除 `hidden md:flex` 类，使导航按钮始终显示
- 添加响应式样式：`text-sm sm:text-lg`、`px-2 sm:px-4`、`gap-1 sm:gap-2`
- 删除移动端汉堡菜单下拉组件
- 添加移动端搜索按钮（图标形式）

### 重要参数

#### 响应式样式对照

| 屏幕尺寸 | 字体大小 | 水平内边距 | 间距 |
|----------|----------|------------|------|
| 手机 (<640px) | `text-sm` | `px-2` | `gap-1` |
| 平板及以上 (≥640px) | `text-lg` | `px-4` | `gap-2` |

### 代码变更

#### 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `astro-melody-starter/src/components/Header.astro` | 移动端导航栏优化 |

#### 修改详情

**Header.astro 修改：**

```diff
- <!-- 桌面端导航链接 -->
- <div class="hidden md:flex items-center justify-center gap-4 mx-auto">
+ <!-- 导航链接 - 始终显示 -->
+ <div class="flex items-center justify-center gap-1 sm:gap-2 mx-auto">
    <a
-     class="inline-flex items-center gap-x-2 rounded-xl border border-transparent px-4 py-2 text-lg font-semibold ..."
+     class="inline-flex items-center gap-x-1 sm:gap-x-2 rounded-xl border border-transparent px-2 sm:px-4 py-1.5 sm:py-2 text-sm sm:text-lg font-semibold ..."
      href="/videos/"
    >
      视频
    </a>
    ...
  </div>

- <!-- 移动端菜单 -->
- <div class="md:hidden dropdown dropdown-end">
-   <div tabindex="0" role="button" ...>
-     <i class="icon-base icon-[tabler--menu-2]"></i>
-   </div>
-   <ul tabindex="0" class="menu dropdown-content ...">
-     <li><a href="/videos/">视频</a></li>
-     ...
-   </ul>
- </div>

+ <!-- 移动端搜索按钮 -->
+ <a class="md:hidden flex size-[38px] items-center justify-center ..." href="/search/">
+   <i class="icon-base icon-[tabler--search]"></i>
+ </a>
```

#### 新增文件

无

#### 删除文件

无

### 修改文件清单

| 文件名 | 相对路径 | 操作类型 |
|--------|----------|----------|
| Header.astro | `astro-melody-starter/src/components/Header.astro` | 修改 |

### 部署信息

- **部署时间**：2026-03-07 03:16:00
- **部署地址**：https://91tutu.cc
- **预览地址**：https://7fe0bc12.astro-melody-starter.pages.dev

---

## v1.0.5 - 集成 IndexNow 搜索引擎即时索引服务 (2026-03-29)

### 功能

1. **IndexNow 集成**
   - 实现搜索引擎即时索引通知功能
   - 支持 Bing、Yandex 搜索引擎
   - 发布新文章时自动通知搜索引擎爬取
   - 提供 API Key 验证文件

2. **自动化通知流程**
   - `generate_markdown.py` 生成文章后自动通知 IndexNow
   - 支持批量 URL 提交（最多 10,000 个）
   - 提供独立通知脚本用于手动通知

### 实现逻辑

#### IndexNow 工作原理

1. **API Key 验证**：在网站根目录放置 `{api-key}.txt` 文件，内容为 API Key 本身
2. **URL 提交**：通过 POST 请求向搜索引擎端点提交 URL 列表
3. **搜索引擎验证**：搜索引擎访问验证文件确认网站所有权
4. **爬取调度**：搜索引擎根据提交的 URL 安排爬取

#### 通知流程

```
generate_markdown.py 执行
    ↓
生成 Markdown 文件
    ↓
收集新生成的 URL
    ↓
POST 请求到 IndexNow 端点
    ↓
Bing/Yandex 收到通知
    ↓
搜索引擎验证 API Key
    ↓
安排爬取新页面
```

### 重要参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `INDEXNOW_KEY` | `de748853d6e24417b898bb2b3d2c62d8` | IndexNow API Key |
| `INDEXNOW_HOST` | `91tutu.cc` | 网站域名 |
| `INDEXNOW_SITE_URL` | `https://91tutu.cc` | 网站完整 URL |

#### IndexNow 端点

| 搜索引擎 | 端点 URL |
|----------|----------|
| Bing | `https://www.bing.com/indexnow` |
| Yandex | `https://yandex.com/indexnow` |
| Naver | `https://searchadvisor.naver.com/indexnow` |

#### API 响应状态码

| HTTP 状态码 | 含义 |
|-------------|------|
| 200 | URL 提交成功 |
| 202 | URL 已接收，等待 Key 验证 |
| 400 | 请求格式错误 |
| 403 | Key 无效或验证失败 |
| 422 | URL 不属于该主机 |
| 429 | 请求过于频繁 |

### 代码变更

#### 新增文件

| 文件路径 | 说明 |
|----------|------|
| `astro-melody-starter/public/de748853d6e24417b898bb2b3d2c62d8.txt` | IndexNow API Key 验证文件 |
| `Image-1/indexnow_notify.py` | 独立 IndexNow 通知脚本 |

#### 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `Image-1/generate_markdown.py` | 集成 IndexNow 自动通知功能 |

#### 修改详情

**generate_markdown.py 新增配置：**

```python
# IndexNow Configuration
INDEXNOW_KEY = "de748853d6e24417b898bb2b3d2c62d8"
INDEXNOW_HOST = "91tutu.cc"
INDEXNOW_SITE_URL = "https://91tutu.cc"
INDEXNOW_ENDPOINTS = [
    "https://www.bing.com/indexnow",
    "https://yandex.com/indexnow",
]
```

**generate_markdown.py 新增通知函数：**

```python
def notify_indexnow(urls):
    """Notify IndexNow about new URLs"""
    if not urls:
        return
    
    payload = {
        "host": INDEXNOW_HOST,
        "key": INDEXNOW_KEY,
        "urlList": urls
    }
    
    for endpoint in INDEXNOW_ENDPOINTS:
        response = requests.post(
            endpoint,
            json=payload,
            headers={"Content-Type": "application/json; charset=utf-8"},
            timeout=30
        )
        # 处理响应...
```

**generate_markdown.py main() 函数修改：**

```diff
+ generated_urls = []

  for folder in folders:
      # ... 生成 Markdown ...
+     generated_urls.append(f"{INDEXNOW_SITE_URL}/posts/{slug}/")

+ # Notify IndexNow about new URLs
+ if generated_urls:
+     notify_indexnow(generated_urls)
```

#### 删除文件

无

### 修改文件清单

| 文件名 | 相对路径 | 操作类型 |
|--------|----------|----------|
| de748853d6e24417b898bb2b3d2c62d8.txt | `astro-melody-starter/public/de748853d6e24417b898bb2b3d2c62d8.txt` | 新增 |
| indexnow_notify.py | `Image-1/indexnow_notify.py` | 新增 |
| generate_markdown.py | `Image-1/generate_markdown.py` | 修改 |

### 使用说明

#### 自动通知（推荐）

运行 `generate_markdown.py` 时会自动通知 IndexNow：

```bash
python C:\huang\Image-1\generate_markdown.py
```

#### 手动通知

使用独立脚本手动通知：

```bash
# 通知单个 URL
python C:\huang\Image-1\indexnow_notify.py https://91tutu.cc/posts/example/

# 通知 sitemap
python C:\huang\Image-1\indexnow_notify.py --sitemap

# 通知多个 URL
python C:\huang\Image-1\indexnow_notify.py --urls url1 url2 url3
```

### 部署信息

- **部署时间**：2026-03-29
- **部署地址**：https://91tutu.cc
- **验证文件地址**：https://91tutu.cc/de748853d6e24417b898bb2b3d2c62d8.txt

---

## v1.0.6 - 新增博文归档页面 (2026-04-04 17:20:00)

### 功能

1. **博文归档页面**
   - 按日期分类显示所有博文标题
   - 每页显示 25 个标题
   - 标题可点击直达对应博文页面
   - 支持分页导航

2. **导航菜单更新**
   - 在 Header 导航中添加"归档"链接
   - 位于"标签"和"留言板"之间

### 实现逻辑

#### 归档页面实现

1. **数据获取**：使用 `getCollection("posts")` 获取所有博文
2. **排序**：按 `pubDate` 降序排列
3. **分组**：按日期（YYYY-MM-DD）分组显示
4. **分页**：每页 25 篇，使用动态路由 `[page].astro`

#### 文件结构

```
src/pages/archives/
├── index.astro      # 归档首页（第1页）
└── [page].astro     # 分页页面
```

### 重要参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `postsPerPage` | 25 | 每页显示博文数量 |
| 分页路由 | `/archives/`, `/archives/2/`, ... | 分页 URL 格式 |

### 代码变更

#### 新增文件

| 文件路径 | 说明 |
|----------|------|
| `astro-melody-starter/src/pages/archives/index.astro` | 归档首页 |
| `astro-melody-starter/src/pages/archives/[page].astro` | 分页页面 |

#### 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `astro-melody-starter/src/components/Header.astro` | 添加"归档"导航链接 |

### 修改文件清单

| 文件名 | 相对路径 | 操作类型 |
|--------|----------|----------|
| index.astro | `astro-melody-starter/src/pages/archives/index.astro` | 新增 |
| [page].astro | `astro-melody-starter/src/pages/archives/[page].astro` | 新增 |
| Header.astro | `astro-melody-starter/src/components/Header.astro` | 修改 |

### 部署信息

- **部署时间**：2026-04-04 17:20:00
- **部署地址**：https://91tutu.cc
- **预览地址**：无

---

## v1.0.7 - 归档页面新增全库搜索功能 (2026-04-04 17:52:00)

### 功能

1. **全库搜索**
   - 突破页数限制，搜索所有归档博文
   - 支持模糊匹配（部分匹配和字符跳跃匹配）
   - 实时搜索（300ms 防抖）

2. **搜索体验优化**
   - 匹配关键词高亮显示
   - 搜索结果按日期分组
   - 清除按钮（X）和 ESC 快捷键
   - 无结果友好提示

### 实现逻辑

#### 搜索架构

1. **API 端点**：`/api/archives.json` 返回所有博文数据
2. **前端搜索**：客户端 JavaScript 实现模糊匹配
3. **防抖处理**：300ms 延迟，避免频繁搜索

#### 模糊匹配算法

```javascript
function fuzzyMatch(text, query) {
  // 1. 直接包含匹配
  if (textLower.includes(queryLower)) return true;
  
  // 2. 字符跳跃匹配
  let queryIndex = 0;
  for (let i = 0; i < text.length && queryIndex < query.length; i++) {
    if (textLower[i] === queryLower[queryIndex]) {
      queryIndex++;
    }
  }
  return queryIndex === query.length;
}
```

### 重要参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 防抖延迟 | 300ms | 输入后延迟搜索时间 |
| API 路径 | `/api/archives.json` | 博文数据 API |

### 代码变更

#### 新增文件

| 文件路径 | 说明 |
|----------|------|
| `astro-melody-starter/src/pages/api/archives.json.ts` | 博文数据 API 端点 |

#### 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `astro-melody-starter/src/pages/archives/index.astro` | 添加搜索框和搜索逻辑 |

### 修改文件清单

| 文件名 | 相对路径 | 操作类型 |
|--------|----------|----------|
| archives.json.ts | `astro-melody-starter/src/pages/api/archives.json.ts` | 新增 |
| index.astro | `astro-melody-starter/src/pages/archives/index.astro` | 修改 |

### 部署信息

- **部署时间**：2026-04-04 17:52:00
- **部署地址**：https://91tutu.cc
- **预览地址**：无

---

## v1.0.8 - 代码备份到 GitHub (2026-04-12 10:00:00)

### 功能

1. **GitHub 代码备份**
   - 创建新分支 `backup-2026-04-12` 用于代码备份
   - 完整备份整个 huang 文件夹内容
   - 保留历史版本，不覆盖旧代码

2. **更新日志维护**
   - 记录本次备份操作
   - 保持版本号递增

### 实现逻辑

1. **创建新分支**：使用 `git checkout -b backup-2026-04-12` 创建备份分支
2. **提交代码**：将当前所有代码提交到新分支
3. **推送远程**：将新分支推送到 GitHub 远程仓库
4. **更新日志**：记录备份操作和相关信息

### 重要参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 分支名称 | `backup-2026-04-12` | 备份分支名称 |
| 备份内容 | 整个 huang 文件夹 | 包含所有代码和资源 |

### 代码变更

#### 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `更新日志.md` | 添加 v1.0.8 版本记录 |

### 修改文件清单

| 文件名 | 相对路径 | 操作类型 |
|--------|----------|----------|
| 更新日志.md | `更新日志.md` | 修改 |

### 部署信息

- **备份时间**：2026-04-12 10:00:00
- **分支名称**：backup-2026-04-12
- **备份内容**：完整 huang 文件夹

---

## v1.0.9 - 弹窗广告可配置化改造 (2026-04-22 23:55:00)

### 功能

1. **弹窗广告开关控制**
   - 将弹窗广告功能改为可通过配置文件控制
   - 在 `consts.ts` 中新增 `POPUNDER_ENABLED` 常量
   - 只需修改一个字符（`true` / `false`）即可开启或关闭弹窗广告

2. **广告配置集中管理**
   - 将所有 ExoClick Popunder 广告配置参数移至 `consts.ts`
   - 包括广告位 ID、频率控制、触发方式等

### 实现逻辑

#### 配置参数集中化

将原本硬编码在组件和脚本中的配置参数提取到全局配置文件：

```typescript
// astro-melody-starter/src/consts.ts
export const POPUNDER_ENABLED = true;  // 改为 false 即可关闭弹窗广告
export const POPUNDER_IDZONE = "5866630";
export const POPUNDER_FREQUENCY_PERIOD = 60;
export const POPUNDER_FREQUENCY_COUNT = 1;
export const POPUNDER_TRIGGER_METHOD = 1;
```

#### 组件中使用配置

**ExoClickPopunder.astro 修改：**

```diff
+ ---
+ import { POPUNDER_ENABLED, POPUNDER_IDZONE, POPUNDER_FREQUENCY_PERIOD, POPUNDER_FREQUENCY_COUNT, POPUNDER_TRIGGER_METHOD } from "@consts";
+ 
+ if (!POPUNDER_ENABLED) {
+   return;
+ }
+ ---

  <script
    is:inline
    type="application/javascript"
    async
-   src="https://a.pemsrv.com/popunder1001.js"
+   src="/popunder.js"
    id="popunderldr"
-   data-idzone="5866630">
+   data-idzone={POPUNDER_IDZONE}
+   data-frequency_period={POPUNDER_FREQUENCY_PERIOD}
+   data-frequency_count={POPUNDER_FREQUENCY_COUNT}
+   data-trigger_method={POPUNDER_TRIGGER_METHOD}
+   data-chrome_enabled="true"
+   data-capping_enabled="true"
+   data-cookieconsent="true">
  </script>
```

**BaseLayout.astro 修改：**

```diff
  import ExoClickPopunder from "@components/ExoClickPopunder.astro";
+ import { POPUNDER_ENABLED } from "@consts";

  // 爬虫检测逻辑...
  
  {!isBot && POPUNDER_ENABLED && <ExoClickPopunder />}
```

#### popunder.js 配置同步

```javascript
// astro-melody-starter/public/popunder.js
var config = {
    idzone: '5866630',
    ads_host: 'a.pemsrv.com',
    syndication_host: 's.pemsrv.com',
    frequency_period: 60,
    frequency_count: 1,
    trigger_method: 1,
    // ... 其他配置
};
```

### 重要参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `POPUNDER_ENABLED` | `true` / `false` | 弹窗广告总开关 |
| `POPUNDER_IDZONE` | `5866630` | ExoClick 广告位 ID |
| `POPUNDER_FREQUENCY_PERIOD` | `60` | 频率限制周期（分钟） |
| `POPUNDER_FREQUENCY_COUNT` | `1` | 周期内最多显示次数 |
| `POPUNDER_TRIGGER_METHOD` | `1` | 触发方式（1=任意点击） |

### 代码变更

#### 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `astro-melody-starter/src/consts.ts` | 新增弹窗广告配置常量 |
| `astro-melody-starter/src/components/ExoClickPopunder.astro` | 改用配置常量，添加开关判断 |
| `astro-melody-starter/src/layouts/BaseLayout.astro` | 添加 POPUNDER_ENABLED 条件渲染 |
| `astro-melody-starter/public/popunder.js` | 配置参数与 consts.ts 保持一致 |

### 修改文件清单

| 文件名 | 相对路径 | 操作类型 |
|--------|----------|----------|
| consts.ts | `astro-melody-starter/src/consts.ts` | 修改 |
| ExoClickPopunder.astro | `astro-melody-starter/src/components/ExoClickPopunder.astro` | 修改 |
| BaseLayout.astro | `astro-melody-starter/src/layouts/BaseLayout.astro` | 修改 |
| popunder.js | `astro-melody-starter/public/popunder.js` | 修改 |

### 使用说明

#### 关闭弹窗广告

只需修改 `consts.ts` 文件中的一行：

```typescript
export const POPUNDER_ENABLED = false;  // 改为 false
```

#### 开启弹窗广告

```typescript
export const POPUNDER_ENABLED = true;  // 改为 true
```

### 部署信息

- **更新时间**：2026-04-22 23:55:00
- **部署地址**：https://91tutu.cc

---

## v1.1.0 - 博文页面集成 ExoClick Native 原生广告 (2026-04-26 15:15:00)

### 功能

1. **博文详情页内嵌原生广告**
   - 在博文详情页第一张图和第二张图之间插入 ExoClick Native 原生广告
   - 广告容器尺寸：300×100px，居中显示
   - 移动端自适应宽度
   - 支持 Astro 客户端路由（View Transitions）

2. **广告组件化封装**
   - 创建 `InArticleAd.astro` 独立组件
   - 使用 class 切换控制显示/隐藏，避免 CSS `!important` 优先级冲突
   - 使用 `<script is:inline>` 确保脚本在客户端正确执行

### 实现逻辑

#### DOM 结构分析

博文内容中每张图片被 `<p>` 标签包裹：

```html
<article class="prose">
  <p><img src="第1张图"></p>
  <p><img src="第2张图"></p>    ← 找到这个 <p>，在其前面插入广告
  <p><img src="第3张图"></p>
  ...
</article>
```

#### 插入策略

1. 遍历 `article.prose` 内所有 `<p>` 元素
2. 找到第2个包含 `<img>` 的 `<p>` 标签
3. 使用 `insertBefore(adContainer, targetP)` 将广告插入到该 `<p>` 之前
4. 将广告容器 class 从 `in-article-ad-hidden` 切换为 `in-article-ad-visible`

#### 为什么不用 CSS `!important` 隐藏

之前版本使用 `display: none !important` 隐藏广告，JS 用 `style.display = 'block'` 无法覆盖（`!important` 优先级高于内联样式）。改用 class 切换方案：

```css
.in-article-ad-hidden { display: none; }
.in-article-ad-visible { display: block; ... }
```

JS 插入成功后执行 `adContainer.className = 'in-article-ad-visible'` 即可。

#### 为什么不直接插入 `<img>` 的 `<p>` 内部

HTML 规范不允许 `<p>` 内包含 `<div>` 等块级元素。如果执行 `img.parentNode.insertBefore(ad, img)`，浏览器会自动修正 DOM，将广告踢出 `<p>`，导致位置不可控。

#### 重试机制

广告容器初始渲染在文章末尾，需要等待内容 DOM 加载后移动。使用递增延迟重试（最多30次，每次300ms），确保在 Astro 客户端路由后也能正确定位。

### 重要参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `data-zoneid` | `5910390` | ExoClick Native 广告位 ID |
| 广告脚本 | `https://a.magsrv.com/ad-provider.js` | ExoClick Ad Provider |
| 广告尺寸（桌面） | 300×100px | 横向矩形 |
| 广告尺寸（移动） | 100% 宽度自适应 | 响应式 |
| 最大重试次数 | 30 | DOM 定位重试 |
| 重试间隔 | 300ms × 尝试次数 | 递增延迟 |

### 代码变更

#### 新增文件

| 文件路径 | 说明 |
|----------|------|
| `astro-melody-starter/src/components/InArticleAd.astro` | 文章内嵌原生广告组件 |

#### 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `astro-melody-starter/src/pages/posts/[...slug].astro` | 导入 InArticleAd 组件，添加到 Content 之后 |

#### 修改详情

**InArticleAd.astro 核心逻辑：**

```javascript
function insertAd() {
  var adContainer = document.getElementById('inArticleAdContainer');
  var allP = document.querySelectorAll('article.prose p');
  var imgPCount = 0;
  var targetP = null;

  for (var i = 0; i < allP.length; i++) {
    if (allP[i].querySelector('img')) {
      imgPCount++;
      if (imgPCount === 2) {
        targetP = allP[i];
        break;
      }
    }
  }

  if (targetP) {
    targetP.parentNode.insertBefore(adContainer, targetP);
    adContainer.className = 'in-article-ad-visible';
    return true;
  }
  return false;
}
```

**[...slug].astro 修改：**

```diff
---
import DownloadAllImages from "@components/DownloadAllImages.astro";
+ import InArticleAd from "@components/InArticleAd.astro";
---

      <div>
        <DownloadAllImages postTitle={post.data.title} />
        <Content />
+       <InArticleAd />
      </div>
```

#### 删除文件

无

### 修改文件清单

| 文件名 | 相对路径 | 操作类型 |
|--------|----------|----------|
| InArticleAd.astro | `astro-melody-starter/src/components/InArticleAd.astro` | 新增 |
| [...slug].astro | `astro-melody-starter/src/pages/posts/[...slug].astro` | 修改 |

### 部署信息

- **更新时间**：2026-04-26 15:15:00
- **部署地址**：https://91tutu.cc
- **预览地址**：https://1cb1f953.astro-melody-starter.pages.dev

---

## v1.1.1 - 弹窗广告修复与文档整理 (2026-04-30)

### 功能

1. **弹窗广告修复（V7 内联方案）**
   - 修复弹窗广告打开 404 错误的问题
   - 从 V8 外部脚本方案切换为 V7 内联代码方案
   - 设置 `only_inline: true` 阻止加载外部脚本
   - 设置 `tcf_enabled: false` 修复 `popMagic.browser` 为 null 的报错
   - 下载弹窗广告使用 `trigger_method: 2` + `trigger_class: "download-pop-trigger"` 精准触发

2. **广告文档整理与转移**
   - 将项目内所有广告相关 MD 文件转移到 `C:\huang\` 根目录
   - 所有文档改为中文文件名
   - 更新文档内容以反映当前 V7 内联实现方案

### 实现逻辑

#### 弹窗广告 404 问题根因

官方 pp.md 提供的 V8 版本 popMagic 代码，其 `buildUrl()` 函数生成的是脚本 URL（`s.pemsrv.com/popunder1000.js?type=inline&...`），而非真实广告 URL。当外部脚本加载失败时，弹窗会打开这个脚本 URL 本身，导致 404。

V7 版本的 `buildUrl()` 直接构建真实广告链接（`s.pemsrv.com/v1/link.php?idzone=...&type=8&...`），不依赖外部脚本即可正常工作。

#### only_inline: true 的作用

- `only_inline: false` 时，`loadHosted()` 会从 `a.pemsrv.com/popunder1000.js` 加载外部脚本
- 外部脚本实际是 V8 版本，会覆盖内联的 V7 代码
- V8 覆盖后 `buildUrl()` 生成错误 URL → 404
- `only_inline: true` 阻止加载外部脚本，V7 内联代码独立运行

#### tcf_enabled: false 的作用

- `tcf_enabled: true` 要求网页有 `__tcfapi` 接口（GDPR 同意框架）
- 网站没有实现该框架，导致 `popMagic.browser` 为 null，后续代码报 TypeError
- `tcf_enabled: false` 跳过 TCF 检查

#### 文档转移对照表

| 原文件路径 | 新文件路径 | 说明 |
|-----------|-----------|------|
| `astro-melody-starter/docs/features-control.md` | `C:\huang\功能开关说明.md` | 功能开关文档 |
| `astro-melody-starter/docs/ads-implementation.md` | `C:\huang\广告实现说明.md` | 广告实现文档 |
| `astro-melody-starter/docs/popunder-usage.md` | `C:\huang\弹窗广告使用说明.md` | 弹窗广告使用说明 |
| `astro-melody-starter/docs/inarticle-ad-usage.md` | `C:\huang\文章内嵌广告使用说明.md` | 文章内嵌广告使用说明 |
| `astro-melody-starter/docs/ad-gated-download-proposal.md` | `C:\huang\广告门控下载方案.md` | 广告门控下载方案 |
| `C:\huang\弹窗广告说明.md` | `C:\huang\弹窗广告说明.md` | 已存在，保留 |

### 重要参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `POPUNDER_ENABLED` | `false` | 全站弹窗广告开关（当前关闭） |
| `DOWNLOAD_POPUNDER_ENABLED` | `true` | 下载弹窗广告开关（当前开启） |
| `only_inline` | `true` | 不加载外部脚本，使用 V7 内联代码 |
| `tcf_enabled` | `false` | 不检查 GDPR 同意框架 |
| `trigger_method` (下载弹窗) | `2` | 仅特定 class 触发 |
| `trigger_class` (下载弹窗) | `download-pop-trigger` | 下载按钮的 CSS 类名 |

### 代码变更

#### 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `astro-melody-starter/src/components/ExoClickPopunder.astro` | 改用 V7 内联代码 + only_inline: true |
| `astro-melody-starter/src/components/DownloadPopunder.astro` | 改用 V7 内联代码 + only_inline: true |
| `astro-melody-starter/src/consts.ts` | 新增下载弹窗配置参数 |

#### 新增文件

| 文件路径 | 说明 |
|----------|------|
| `C:\huang\功能开关说明.md` | 功能开关文档（从 docs/features-control.md 转移并更新） |
| `C:\huang\广告实现说明.md` | 广告实现文档（从 docs/ads-implementation.md 转移并更新） |
| `C:\huang\弹窗广告使用说明.md` | 弹窗广告使用说明（从 docs/popunder-usage.md 转移并更新） |
| `C:\huang\文章内嵌广告使用说明.md` | 文章内嵌广告使用说明（从 docs/inarticle-ad-usage.md 转移并更新） |
| `C:\huang\广告门控下载方案.md` | 广告门控下载方案（从 docs/ad-gated-download-proposal.md 转移并更新） |

#### 删除文件

| 文件路径 | 说明 |
|----------|------|
| `astro-melody-starter/docs/features-control.md` | 已转移到 C:\huang\功能开关说明.md |
| `astro-melody-starter/docs/ads-implementation.md` | 已转移到 C:\huang\广告实现说明.md |
| `astro-melody-starter/docs/popunder-usage.md` | 已转移到 C:\huang\弹窗广告使用说明.md |
| `astro-melody-starter/docs/inarticle-ad-usage.md` | 已转移到 C:\huang\文章内嵌广告使用说明.md |
| `astro-melody-starter/docs/ad-gated-download-proposal.md` | 已转移到 C:\huang\广告门控下载方案.md |

### 修改文件清单

| 文件名 | 相对路径 | 操作类型 |
|--------|----------|----------|
| ExoClickPopunder.astro | `astro-melody-starter/src/components/ExoClickPopunder.astro` | 修改 |
| DownloadPopunder.astro | `astro-melody-starter/src/components/DownloadPopunder.astro` | 修改 |
| consts.ts | `astro-melody-starter/src/consts.ts` | 修改 |
| 功能开关说明.md | `功能开关说明.md` | 新增 |
| 广告实现说明.md | `广告实现说明.md` | 新增 |
| 弹窗广告使用说明.md | `弹窗广告使用说明.md` | 新增 |
| 文章内嵌广告使用说明.md | `文章内嵌广告使用说明.md` | 新增 |
| 广告门控下载方案.md | `广告门控下载方案.md` | 新增 |
| 更新日志.md | `更新日志.md` | 修改 |

### 部署信息

- **更新时间**：2026-04-30
- **部署地址**：https://91tutu.cc

---

## v1.1.2 - 下载弹窗改为直接重定向方式 (2026-04-30)

### 功能

1. **下载弹窗广告改为直接重定向方式**
   - 从 popMagic V7 内联代码改为直接 `window.open()` 打开广告重定向 URL
   - 使用新的广告位 ID `5914280`（popunder 重定向类型）
   - 每次点击下载按钮都会弹窗，频率由广告商控制
   - 删除不再需要的 `DOWNLOAD_POPUNDER_FREQUENCY_PERIOD` 和 `DOWNLOAD_POPUNDER_FREQUENCY_COUNT` 配置

2. **文档同步更新**
   - 更新所有广告相关 MD 文件，反映新的直接重定向实现方式

### 实现逻辑

#### 为什么要改

之前下载弹窗使用 popMagic V7 内联代码，存在两个问题：
1. **Cookie 频率限制**：popMagic 用 Cookie 控制频率，但广告商服务端也有自己的频率限制，两边限制叠加导致"只弹一次"
2. **代码复杂**：popMagic 代码几百行，而下载弹窗只需要"点击按钮→打开广告URL"

改为直接重定向方式后：
- 每次点击都打开 `https://s.pemsrv.com/v1/link.php?cat=&idzone=5914280&type=8&p=...&cb=...`
- 频率完全由广告商控制
- 代码从 ~300行简化到 ~20行

#### 广告 URL 参数

| 参数 | 是否必须 | 说明 |
|------|---------|------|
| `idzone` | ✅ 必须 | 广告位 ID，决定收益归属 |
| `type` | ✅ 必须 | 广告类型，8=popunder 重定向 |
| `p` | 可选 | 来源页面 URL，帮助广告商统计 |
| `cb` | 可选 | 随机数，防止浏览器缓存 |

### 重要参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `DOWNLOAD_POPUNDER_ENABLED` | `true` | 下载弹窗开关 |
| `DOWNLOAD_POPUNDER_IDZONE` | `"5914280"` | 新的广告位 ID（之前是 5866630） |

### 代码变更

#### 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `astro-melody-starter/src/consts.ts` | idzone 改为 5914280，删除 FREQUENCY_PERIOD 和 FREQUENCY_COUNT |
| `astro-melody-starter/src/components/DownloadPopunder.astro` | 从 popMagic V7 内联代码改为直接重定向方式 |

#### 修改详情

**consts.ts 修改：**

```diff
  export const DOWNLOAD_POPUNDER_ENABLED = true;
- export const DOWNLOAD_POPUNDER_IDZONE = "5866630";
- export const DOWNLOAD_POPUNDER_FREQUENCY_PERIOD = 0;
- export const DOWNLOAD_POPUNDER_FREQUENCY_COUNT = 999999;
+ export const DOWNLOAD_POPUNDER_IDZONE = "5914280";
```

**DownloadPopunder.astro 重写：**

```diff
- // popMagic V7 内联代码（~300行）
+ // 直接重定向方式（~20行）
+ function openPopunderAd() {
+     var adUrl = "https://s.pemsrv.com/v1/link.php?cat=&idzone="
+         + dlIdzone + "&type=8&p="
+         + encodeURIComponent(window.location.href)
+         + "&cb=" + Math.floor(Math.random() * 1e9);
+     window.open(adUrl, "_blank");
+ }
```

### 修改文件清单

| 文件名 | 相对路径 | 操作类型 |
|--------|----------|----------|
| consts.ts | `astro-melody-starter/src/consts.ts` | 修改 |
| DownloadPopunder.astro | `astro-melody-starter/src/components/DownloadPopunder.astro` | 重写 |
| 功能开关说明.md | `功能开关说明.md` | 修改 |
| 广告实现说明.md | `广告实现说明.md` | 修改 |
| 弹窗广告使用说明.md | `弹窗广告使用说明.md` | 修改 |
| 广告门控下载方案.md | `广告门控下载方案.md` | 修改 |

### 部署信息

- **更新时间**：2026-04-30
- **部署地址**：https://91tutu.cc
- **预览地址**：https://4e139a0f.astro-melody-starter.pages.dev

---

## v1.2.0 - 博文列表内嵌伪装广告覆盖层 (2026-05-01)

### 功能

1. **博文列表随机内嵌伪装广告**
   - 在每页博文列表中，随机选取一个正常博文卡片，覆盖一层伪装广告
   - 伪装广告外观与正常博文卡片完全一致（随机封面、标题、标签）
   - 用户点击伪装卡片后，触发 ExoClick 原生广告，然后覆盖层自动消失，露出原始博文卡片
   - 首页前3个博文（HeroCard + 2个特色卡片）不会被替换，确保核心内容不受影响

2. **覆盖层（Overlay）方案**
   - 原始博文卡片完全不动，只在上方叠加一个覆盖层
   - 覆盖层使用与 AdWidgetTriple 完全相同的双层结构（广告层 + 伪装内容层）
   - 点击后 500ms 延迟移除覆盖层，确保广告正常处理

### 实现逻辑

#### 核心流程

```
页面加载
  ↓
扫描所有博文卡片（通过 data-view-count 属性定位）
  ↓
跳过首页前3个卡片（skipFirst=3）
  ↓
从剩余卡片中随机选取1个
  ↓
在该卡片上方插入覆盖层（position: absolute, z-index: 20）
  ↓
覆盖层内：
  - ad-layer (z-0): ExoClick <ins> 广告（与 AdWidgetTriple 相同）
  - content-layer (z-10): 伪装封面/标题/标签（pointer-events: none）
  ↓
初始化 AdProvider 加载广告
  ↓
用户点击覆盖层 → 广告正常触发
  ↓
500ms 后覆盖层 remove() → 露出原始博文卡片
```

#### 覆盖层结构

```
┌─────────────────────────────────────┐
│  原始博文卡片（不动）                │
│  ┌─────────────────────────────────┐│
│  │  inline-ad-overlay (z-20)       ││  ← 覆盖层
│  │  ┌───────────────────────────┐  ││
│  │  │  content-layer (z-10)     │  ││  ← 伪装封面/标题（pointer-events: none）
│  │  └───────────────────────────┘  ││
│  │  ┌───────────────────────────┐  ││
│  │  │  ad-layer (z-0)           │  ││  ← ExoClick <ins> 广告
│  │  └───────────────────────────┘  ││
│  └─────────────────────────────────┘│
└─────────────────────────────────────┘
```

#### 为什么用覆盖层而不是替换

| 方案 | 问题 |
|------|------|
| 替换卡片内容 | `<template>` 内的 `<script>` 不执行，广告无法加载 |
| 替换后用 window.open 打开广告 URL | ExoClick 的 ddn.php 直接访问返回 404 |
| **覆盖层方案** ✅ | 原始卡片不动，广告用 `<ins>` 正常加载，点击正常触发，覆盖层消失即可 |

### 重要参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 广告 Zone ID | `5863494` | 与 AdWidgetTriple 相同 |
| 广告商 JS | `https://a.magsrv.com/ad-provider.js` | ExoClick 广告 SDK |
| ins class | `eas6a97888e20` | 与 AdWidgetTriple 相同 |
| 首页 skipFirst | `3` | 跳过 HeroCard + 2个特色卡片 |
| 其他页面 skipFirst | `0` | 分类页、标签页不跳过 |
| 点击后延迟 | `500ms` | 覆盖层移除延迟，确保广告处理完成 |

### 代码变更

#### 新增文件

| 文件路径 | 说明 |
|----------|------|
| `astro-melody-starter/src/components/InlineAdReplacer.astro` | 内嵌伪装广告覆盖层组件 |

#### 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `astro-melody-starter/src/pages/index.astro` | 引入 InlineAdReplacer，skipFirst=3 |
| `astro-melody-starter/src/pages/page/[page].astro` | 引入 InlineAdReplacer，首页 skipFirst=3，其他页 skipFirst=0 |
| `astro-melody-starter/src/pages/category/[category]/[page].astro` | 引入 InlineAdReplacer，skipFirst=0 |
| `astro-melody-starter/src/pages/tags/[tag]/[page].astro` | 引入 InlineAdReplacer，skipFirst=0 |

#### InlineAdReplacer.astro 组件说明

| 代码部分 | 作用 |
|----------|------|
| `postsData` | 从博文列表提取轻量数据（id, cover, tags, title, pubDate） |
| `initInlineAd()` | 主函数：扫描卡片 → 随机选取 → 创建覆盖层 → 初始化广告 |
| `fillOverlay()` | 用随机博文数据填充伪装内容层 |
| `overlay.addEventListener('click')` | 点击后 500ms 移除覆盖层 |
| CSS `.inline-ad-overlay` | 覆盖层样式，隐藏 ExoClick 原生广告自身 UI |

### 修改文件清单

| 文件名 | 相对路径 | 操作类型 |
|--------|----------|----------|
| InlineAdReplacer.astro | `astro-melody-starter/src/components/InlineAdReplacer.astro` | 新增 |
| index.astro | `astro-melody-starter/src/pages/index.astro` | 修改 |
| [page].astro | `astro-melody-starter/src/pages/page/[page].astro` | 修改 |
| [page].astro | `astro-melody-starter/src/pages/category/[category]/[page].astro` | 修改 |
| [page].astro | `astro-melody-starter/src/pages/tags/[tag]/[page].astro` | 修改 |

### 部署信息

- **更新时间**：2026-05-01
- **部署地址**：https://91tutu.cc

---

## v1.3.2 - 页面标题 SEO 后缀 + 新增 10 篇博文 (2026-05-03)

### 功能

1. **页面标题 SEO 后缀**
   - 所有非首页的浏览器标签标题后自动追加 `-91图图网-分享精选优质写真福利套图，视频全收集|Cosplayl二次元|写真|福利|图包|美图`
   - 首页标题保持不变（已是完整站点名）
   - 涉及 `<title>`、`og:title`、`twitter:title`、Schema.org `name` 全部统一

2. **新增 10 篇博文**
   - 阿雪雪 - 碧蓝航线 能代 旗袍 [96P,6V-5.82GB]
   - 葛生 - No.015 真空玫瑰 [11P,1V-18MB]
   - 胡桃猫 - 甘雨魅魔 [73P-813MB]
   - 麻薯大王 - No.016 瑜伽垫上的越界 [75P,1V-838MB]
   - 麻薯大王 - No.017 绒茸甜心 [40P-463MB]
   - 麻薯大王 - No.018 惊喜掉落彩蛋 [35P-395MB]
   - 神沢永莉 - No.033 ckt儿童节快乐 [68P,1V-387MB]
   - 雯妹不讲道理 - No.118 温柔女友 [47P-165MB]
   - 雪晴Astra - No.105 猫猫妹抖 [77P,1V-1.19GB]
   - 芝心蛋奶烧 - No.003 埃吉尔 [34P-355MB]

### 实现逻辑

#### 页面标题后缀

```
BaseHead.astro 接收 title prop
  ↓
判断 title === META_TITLE ?
  是 → fullTitle = title（首页不重复追加）
  否 → fullTitle = title + SITE_SUFFIX
  ↓
<title>、og:title、twitter:title、Schema.org name 全部使用 fullTitle
```

### 重要参数

| 参数 | 值 | 说明 |
|------|-----|------|
| SITE_SUFFIX | `-91图图网-分享精选优质写真福利套图，视频全收集\|Cosplayl二次元\|写真\|福利\|图包\|美图` | 页面标题后缀 |
| META_TITLE | `91图图网-分享精选优质写真福利套图，视频全收集 \|Cosplay\|二次元\|写真\|福利\|图包\|美图` | 首页标题（不追加后缀） |

### 代码变更

#### 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `astro-melody-starter/src/components/BaseHead.astro` | 新增 SITE_SUFFIX 常量，title 替换为 fullTitle |

### 修改文件清单

| 文件名 | 相对路径 | 操作类型 |
|--------|----------|----------|
| BaseHead.astro | `astro-melody-starter/src/components/BaseHead.astro` | 修改 |

### 备份信息

- **备份分支**：`backup-2026-05-03-pre-vast`
- **更新时间**：2026-05-03
- **部署地址**：https://91tutu.cc
- **GitHub 分支**：`feature/inline-ad-overlay`

---

## v1.3.0 - 博文/视频评论区功能 (2026-05-01)

### 功能

1. **博文评论区**
   - 每篇博文底部新增评论区，支持发表评论
   - 每个视频页面也新增评论区
   - 每篇博文/视频的评论区独立，互不影响

2. **昵称系统**
   - 昵称为选填项，不填则自动随机生成
   - 随机昵称格式：`形容词+动物+字母+数字`，如"快乐小熊A42"、"聪明猫咪B37"
   - 通过 `localStorage` 记住用户昵称，下次自动填充
   - 昵称最多20字

3. **点赞功能**
   - 每条评论可点赞，显示点赞数
   - 同一用户对同一评论只能点一次赞（localStorage + 服务端 KV 双重防重复）
   - 采用乐观更新策略：点击后立即更新 UI，再异步发送请求，避免卡顿感
   - 如果服务端返回失败，自动回滚 UI

4. **评论规则（与留言板一致）**
   - 每分钟每IP每博文只能提交1条评论
   - 内容长度 2-500 字
   - 请文明发言，禁止刷屏

### 实现逻辑

#### 数据存储

- 使用 Cloudflare KV 存储评论数据
- KV 命名空间：`POST_COMMENTS`（ID: `e5fe071d2e6f48769eec01fb63e59b3e`）
- 每篇博文的评论存储在 key `comments:{postId}` 下
- 每个视频的评论存储在 key `comments:video-{videoId}` 下
- 点赞防重复存储在 key `like_limit:{fingerprint}:{commentId}` 下（TTL: 365天）
- 频率限制存储在 key `rate_limit:{ip}:{postId}` 下（TTL: 60秒）

#### API 端点

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api/post-comments?postId=xxx&page=1&limit=10` | 获取评论列表（分页） |
| POST | `/api/post-comments` | 发表评论 |
| PUT | `/api/post-comments` | 点赞评论 |

#### 乐观更新（点赞）

```
用户点击点赞按钮
  ↓
立即：markLiked() → localStorage 标记已赞
立即：UI 更新（红心 + 计数+1）
  ↓
异步：fetch PUT /api/post-comments
  ↓
成功 → 用服务端真实计数更新（修正可能的偏差）
失败 → 回滚 UI（恢复原计数 + 灰心）
```

#### 用户标识

| 标识 | 存储位置 | 用途 |
|------|----------|------|
| `post_comment_fp` | localStorage | 用户指纹，用于点赞防重复 |
| `post_comment_nickname` | localStorage | 记住用户昵称 |
| `post_comment_liked` | localStorage | 已点赞的评论ID列表（逗号分隔） |

### 重要参数

| 参数 | 值 | 说明 |
|------|-----|------|
| KV 绑定名 | `POST_COMMENTS` | Cloudflare KV 命名空间绑定名 |
| KV ID | `e5fe071d2e6f48769eec01fb63e59b3e` | Cloudflare KV 命名空间 ID |
| 评论长度限制 | 2-500 字 | 与留言板一致 |
| 频率限制 | 60秒/条/IP/博文 | 与留言板一致 |
| 昵称长度限制 | 最多20字 | - |
| 点赞防重复 TTL | 365天 | KV 中 like_limit key 的过期时间 |
| 每页评论数 | 10条 | 分页加载 |

### 代码变更

#### 新增文件

| 文件路径 | 说明 |
|----------|------|
| `astro-melody-starter/src/pages/api/post-comments.ts` | 博文评论 API 端点（GET/POST/PUT） |
| `astro-melody-starter/src/components/PostComments.astro` | 博文评论区组件 |

#### 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `astro-melody-starter/src/pages/posts/[...slug].astro` | 引入 PostComments 组件，传入 postId |
| `astro-melody-starter/src/pages/videos/[series]/[slug].astro` | 引入 PostComments 组件，传入 video-{videoId} |
| `astro-melody-starter/wrangler.jsonc` | 添加 POST_COMMENTS KV 绑定 |
| `astro-melody-starter/wrangler.toml` | 添加 POST_COMMENTS KV 绑定（含预览环境） |

### 修改文件清单

| 文件名 | 相对路径 | 操作类型 |
|--------|----------|----------|
| post-comments.ts | `astro-melody-starter/src/pages/api/post-comments.ts` | 新增 |
| PostComments.astro | `astro-melody-starter/src/components/PostComments.astro` | 新增 |
| [...slug].astro | `astro-melody-starter/src/pages/posts/[...slug].astro` | 修改 |
| [slug].astro | `astro-melody-starter/src/pages/videos/[series]/[slug].astro` | 修改 |
| wrangler.jsonc | `astro-melody-starter/wrangler.jsonc` | 修改 |
| wrangler.toml | `astro-melody-starter/wrangler.toml` | 修改 |

### 部署信息

- **更新时间**：2026-05-01
- **部署地址**：https://91tutu.cc
