# 弹窗广告实现说明 — 从踩坑到解决

## 一、背景：官方给了什么代码？

你在 `pp.md` 里收到的是一段来自广告商（ExoClick/pemsrv）的弹窗广告代码。这段代码的核心是一个叫 **popMagic** 的程序，它的作用是：

1. **检测用户点击** — 当用户在网页上点击时，触发弹窗
2. **生成广告链接** — 向广告商服务器请求一个真实的广告页面地址
3. **打开弹窗** — 在后台打开这个广告页面，用户看到弹窗，广告商记录一次有效展示，你获得收益

官方代码的版本号是 **8.0.0**（代码里写着 `//version 8.0.0`），我们称之为 **V8**。

---

## 二、参考网站用的是什么？

你让我参考的"91绅士"网站，也用了同一家广告商的弹窗代码，但它的版本号是 **7.0.0**，我们称之为 **V7**。

V7 和 V8 看起来很像，但有一个**致命的区别**，这就是后来所有问题的根源。

---

## 三、V7 和 V8 的致命区别

### 打个比方

想象你开了一家店，需要挂一个广告牌。

- **V7 的做法**：你先在店里准备好广告牌的内容（广告链接），然后把广告牌挂出去。顾客看到的就是真实的广告。
- **V8 的做法**：你先挂一个"广告牌制作中"的牌子，等广告商把真正的广告牌送过来再替换。但如果广告商送不过来，顾客看到的就是"制作中"的空牌子。

### 技术上的区别

| 对比项 | V7（参考网站） | V8（官方代码） |
|--------|---------------|---------------|
| 广告链接怎么生成 | **自己直接生成**真实广告URL：`s.pemsrv.com/v1/link.php?idzone=...` | 先生成一个**脚本URL**：`s.pemsrv.com/popunder1000.js?type=inline&...`，等外部脚本加载后再替换 |
| 需要加载外部脚本吗 | 可以选择不加载（`only_inline: true`） | 默认需要加载外部脚本（`only_inline: false`） |
| 外部脚本可用时 | 正常工作 | 正常工作 |
| 外部脚本不可用时 | **仍然正常工作**（因为自己能生成广告链接） | **弹窗打开的是脚本URL本身 → 404 错误** |

简单说：**V8 依赖外部脚本来生成正确的广告链接，如果外部脚本加载失败，弹窗就会打开一个错误的地址（脚本自身的URL），导致 404。V7 不依赖外部脚本，自己就能生成正确的广告链接。**

---

## 四、踩坑全过程

### 第一坑：弹窗打开后没有内容

**原因**：最初我直接使用了 V8 官方代码，但设置了 `only_inline: true`（不加载外部脚本）。V8 的内联代码在 `only_inline: true` 模式下，`buildUrl()` 方法生成的 URL 是脚本自身的地址，不是广告页面地址。所以弹窗打开的是 `popunder1000.js` 这个脚本文件，而不是广告页面。

**现象**：弹窗打开了，但里面是空白或乱码。

### 第二坑：TypeError: Cannot read properties of null

**原因**：V8 代码中有一个 `tcf_enabled: true` 的设置，这是欧洲隐私法规（GDPR）相关的功能。它要求网页上有一个叫 `__tcfapi` 的接口来获取用户同意。但你的网站上没有这个接口，导致 `popMagic.browser` 始终为 null，后续代码读取 `.name` 时就报错了。

**修复**：把 `tcf_enabled` 改为 `false`，跳过这个检查。

### 第三坑：弹窗显示 404 ERROR

**原因**：把 `only_inline` 改回 `false` 后，V8 代码会尝试从 `a.pemsrv.com/popunder1000.js` 加载外部脚本。但这个外部脚本在你的网站上返回 404（可能是域名验证失败、网络问题等原因）。外部脚本加载失败，`popMagic.url` 没有被替换成真实广告链接，弹窗打开的仍然是脚本URL → 404。

**现象**：弹窗打开的地址是 `https://s.pemsrv.com/popunder1000.js?type=inline&name=popMagic&ver=8&...`，显示 404 ERROR。

### 第四坑：V8 外部脚本覆盖了 V7 内联代码

**原因**：我改用 V7 的内联代码（参考网站的方式），但保留了 `only_inline: false`。V7 的 `init()` 方法在 `only_inline: false` 时会调用 `loadHosted()`，从 `a.pemsrv.com/popunder1000.js` 加载外部脚本。这个外部脚本实际上是 **V8 版本**，它加载后会**覆盖** V7 的代码，重新用 V8 的逻辑运行。结果又回到了 V8 的问题 — 弹窗URL变成了 `popunder1000.js?ver=8&...` → 404。

**现象**：弹窗URL中出现了 `ver=8`，证明外部 V8 脚本覆盖了内联 V7 代码。

### 最终修复

**设置 `only_inline: true`**，彻底阻止加载外部脚本。V7 内联代码独立运行，`buildUrl()` 直接生成正确的广告链接 `s.pemsrv.com/v1/link.php?idzone=...`，弹窗正常显示广告内容。

---

## 五、为什么参考网站用 V7 没问题？

参考网站（91绅士）使用的是 V7 + `only_inline: false`，它的 `loadHosted()` 会加载外部脚本，但外部脚本加载后不会覆盖 V7 的核心逻辑（因为 V7 的外部脚本就是 V7 版本的）。而我们的网站加载外部脚本时，拿到的是 V8 版本，V8 会覆盖 V7 的代码，导致问题。

**关键区别**：参考网站的外部脚本版本和内联代码版本一致（都是 V7），不会互相冲突。而我们的内联代码是 V7，外部脚本却是 V8，版本不一致导致覆盖冲突。

---

## 六、最终方案总结

| 设置项 | 值 | 原因 |
|--------|---|------|
| popMagic 版本 | V7（7.0.0） | V7 能自己生成正确的广告链接，不依赖外部脚本 |
| only_inline | true | 阻止加载外部 V8 脚本，避免版本冲突 |
| tcf_enabled | false | 网站没有 GDPR 同意框架，开启会导致报错 |
| cookieconsent | true | 保留 cookie 同意机制，确保频率控制正常 |

---

## 七、如何修改弹窗参数

所有参数都在 `src/consts.ts` 文件中，修改后重新构建部署即可：

### 下载弹窗（当前已开启）

```typescript
export const DOWNLOAD_POPUNDER_ENABLED = true;           // 开关：true=开启，false=关闭
export const DOWNLOAD_POPUNDER_IDZONE = "5866630";       // 你的广告区ID，不要改
export const DOWNLOAD_POPUNDER_FREQUENCY_PERIOD = 60;    // 频率周期（分钟）
export const DOWNLOAD_POPUNDER_FREQUENCY_COUNT = 999999; // 周期内弹窗次数上限
```

### 全站弹窗（当前已关闭）

```typescript
export const POPUNDER_ENABLED = false;                   // 开关：true=开启，false=关闭
export const POPUNDER_IDZONE = "5866630";                // 你的广告区ID，不要改
export const POPUNDER_FREQUENCY_PERIOD = 60;             // 频率周期（分钟）
export const POPUNDER_FREQUENCY_COUNT = 1;               // 周期内弹窗次数上限
export const POPUNDER_TRIGGER_METHOD = 1;                // 触发方式：1=全页面点击，3=仅链接点击
```

### 参数说明

| 参数 | 含义 | 示例 |
|------|------|------|
| FREQUENCY_PERIOD | 多长时间内（分钟）限制弹窗次数 | 60 = 1小时内 |
| FREQUENCY_COUNT | 在 FREQUENCY_PERIOD 时间内最多弹几次 | 999999 = 几乎无限次 |
| TRIGGER_METHOD | 1 = 点击页面任何地方都触发；2 = 仅点击特定按钮触发；3 = 仅点击链接触发 | 下载弹窗固定用2 |

### 修改后操作

修改 `consts.ts` 后，需要重新构建并部署：

```bash
npm run build
npx wrangler pages deploy dist --project-name=astro-melody-starter --branch=main
```

---

## 八、一句话总结

官方给的 V8 代码依赖外部脚本生成广告链接，但外部脚本在你的网站上加载失败，导致弹窗打开的是脚本地址而非广告地址（404）。改用 V7 代码 + `only_inline: true`（纯内联模式），让弹窗代码自己生成正确的广告链接，不再依赖外部脚本，问题彻底解决。
