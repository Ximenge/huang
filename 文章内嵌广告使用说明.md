# 文章内嵌广告使用说明

## 1. 功能概述

在博文详情页的**第一张图和第二张图之间**自动插入 ExoClick Native 原生广告。

广告以横向矩形（300×100px）居中展示，移动端自适应宽度。

## 2. 广告位信息

| 参数 | 值 |
|------|-----|
| 广告提供商 | ExoClick |
| 广告类型 | Native 原生广告 |
| Zone ID | `5910390` |
| 广告脚本 | `https://a.magsrv.com/ad-provider.js` |
| 广告尺寸（桌面） | 300×100px |
| 广告尺寸（移动） | 100% 宽度自适应 |

## 3. 组件文件

| 文件 | 路径 |
|------|------|
| 广告组件 | `astro-melody-starter/src/components/InArticleAd.astro` |
| 博文页面 | `astro-melody-starter/src/pages/posts/[...slug].astro` |

## 4. 工作原理

### 插入流程

```
页面加载
  ↓
广告容器初始渲染在文章末尾（display: none）
  ↓
JS 遍历所有 <p>，找到第2个包含 <img> 的 <p>
  ↓
insertBefore(adContainer, targetP) 将广告移到该 <p> 前面
  ↓
className 切换为 in-article-ad-visible，广告显示
  ↓
最终效果：第1张图 → 广告 → 第2张图 → ...
```

### 重试机制

- 最多尝试 30 次
- 每次间隔 300ms
- 成功后立即停止重试

## 5. 修改广告位 ID

如需更换广告位，修改 `InArticleAd.astro` 中的 `data-zoneid`：

```html
<ins class="eas6a97888e2" data-zoneid="新的ZONE_ID"></ins>
```

## 6. 调整广告位置

修改 `insertAd()` 函数中的计数条件：

```javascript
// 第2张图之前插入（默认）
if (imgPCount === 2) { targetP = allP[i]; break; }

// 改为第3张图之前插入
if (imgPCount === 3) { targetP = allP[i]; break; }
```

## 7. 调整广告尺寸

修改 `InArticleAd.astro` 中的 CSS：

```css
.in-article-ad-visible {
  max-width: 300px;      /* 修改宽度 */
  max-height: 100px;     /* 修改高度 */
  margin: 20px auto;     /* 修改外边距 */
}
```

## 8. 禁用广告

在 `[...slug].astro` 中注释掉组件：

```astro
<!-- <InArticleAd /> -->
```

## 9. 与其他广告组件的关系

| 组件 | 类型 | 位置 | Zone ID |
|------|------|------|---------|
| ExoClickPopunder.astro | Popunder 弹窗 | 全局 | 5866630 |
| DownloadPopunder.astro | Popunder 弹窗 | 博文页 | 5866630 |
| AdWidget.astro | Native 横幅 | 首页卡片 | 5863494 |
| AdWidgetTriple.astro | Native 三联 | 首页卡片 | 5863494 |
| BottomAd.astro | 底部悬浮 | 全局底部 | 5863710 |
| **InArticleAd.astro** | **Native 原生** | **博文图间** | **5910390** |

## 10. 常见问题

### Q: 广告不显示？

1. 检查 Zone ID 是否正确
2. ExoClick 新广告位需要 15分钟~24小时 生效
3. 打开浏览器开发者工具，查看 Console 是否有报错
4. 检查广告拦截器是否开启

### Q: 广告位置不对？

1. 确认博文内容中有至少 2 张图片
2. 检查图片是否被 `<p>` 标签包裹
3. 如果图片结构不同，需调整 `insertAd()` 中的选择器逻辑

### Q: Astro 页面导航后广告消失？

组件已监听 `astro:page-load` 事件，导航后会重新执行插入逻辑。
