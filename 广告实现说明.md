# 广告实现说明

本文档详细记录项目中所有广告的实现方案，包括代码结构、核心函数、触发逻辑、样式控制和维护要点。

---

## 广告总览

| 广告类型 | Zone ID | 广告商 | 展示位置 | 触发方式 | 可控开关 |
|---------|---------|-------|---------|---------|---------|
| 全站弹窗广告 (Popunder) | 5866630 | ExoClick/pemsrv | 全站所有页面 | 页面任意点击 | `POPUNDER_ENABLED` |
| 下载弹窗广告 (Download Popunder) | 5914280 | ExoClick/pemsrv | 博文详情页 | 点击下载按钮 | `DOWNLOAD_POPUNDER_ENABLED` |
| 文章内嵌广告 (In-Article Native) | 5910390 | ExoClick/pemsrv | 博文详情页文章内 | 自动插入第1-2张图之间 | 无（始终显示） |
| 首页信息流广告 (AdWidget) | 5863494 | ExoClick/pemsrv | 首页信息流中间 | 自动渲染 | 无（始终显示） |
| 首页伪装广告 (AdWidgetTriple) | 5863494 | ExoClick/pemsrv | 首页信息流中间 | 自动渲染+博文封面伪装 | 无（始终显示） |
| 底部悬浮广告 (Bottom Ad) | 5863710 | ExoClick/pemsrv | 全站底部悬浮 | 页面加载后自动显示 | 无（始终显示，爬虫除外） |

---

## 一、全站弹窗广告 (Popunder)

### 文件结构

| 文件 | 作用 |
|------|------|
| `src/consts.ts` | 配置：开关、Zone ID、频率参数 |
| `src/components/ExoClickPopunder.astro` | Astro 组件：内联 V7 popMagic 代码 |
| `src/layouts/BaseLayout.astro` | 渲染入口：全局布局中引入组件 |

### 配置参数

```typescript
// src/consts.ts
export const POPUNDER_ENABLED = false;           // 全局开关
export const POPUNDER_IDZONE = "5866630";        // 广告位 ID
export const POPUNDER_FREQUENCY_PERIOD = 60;      // 频率周期（分钟）
export const POPUNDER_FREQUENCY_COUNT = 1;        // 周期内最大触发次数
export const POPUNDER_TRIGGER_METHOD = 1;         // 触发方式（1=任意点击）
```

### 实现方式

**ExoClickPopunder.astro** — 使用 V7 版本 popMagic 内联代码 + `only_inline: true`：

```astro
---
import { POPUNDER_ENABLED, POPUNDER_IDZONE, POPUNDER_FREQUENCY_PERIOD,
         POPUNDER_FREQUENCY_COUNT, POPUNDER_TRIGGER_METHOD } from "@consts";

if (!POPUNDER_ENABLED) {
  return;
}

const puIdzone = POPUNDER_IDZONE;
const puFreqPeriod = POPUNDER_FREQUENCY_PERIOD;
const puFreqCount = POPUNDER_FREQUENCY_COUNT;
const puTriggerMethod = POPUNDER_TRIGGER_METHOD;
---

<script is:inline define:vars={{ puIdzone, puFreqPeriod, puFreqCount, puTriggerMethod }}>
(function() {
    // V7 popMagic 内联代码
    var adConfig = {
        "ads_host": "a.pemsrv.com",
        "syndication_host": "s.pemsrv.com",
        "idzone": puIdzone,
        "popup_fallback": false,
        "popup_force": false,
        "chrome_enabled": true,
        "new_tab": false,
        "frequency_period": puFreqPeriod,
        "frequency_count": puFreqCount,
        "trigger_method": puTriggerMethod,
        "trigger_class": "",
        "trigger_delay": 0,
        "capping_enabled": true,
        "tcf_enabled": false,
        "only_inline": true,
        "cookieconsent": true
    };
    // ... popMagic V7 完整代码 ...
    popMagic.init(adConfig);
})();
</script>
```

### 渲染位置

**BaseLayout.astro** — 仅对非爬虫用户渲染：

```astro
import { POPUNDER_ENABLED } from "@consts";
const isBot = botUserAgents.some(bot => userAgent.toLowerCase().includes(bot));

{!isBot && POPUNDER_ENABLED && <ExoClickPopunder />}
```

### 核心脚本逻辑（V7 popMagic 内联）

#### 初始化流程

```
页面加载
  → popMagic.init(adConfig) 读取 define:vars 注入的配置
  → 设置 cookie_name = 'zone-cap-' + idzone
  → only_inline: true → 不加载外部脚本
  → checkTCFConsent() → tcf_enabled: false → 直接回调
  → preparePopWait() → 延迟 400ms → preparePop()
  → buildUrl() 构建真实广告 URL
  → addEvent() 根据 trigger_method 绑定点击事件
```

#### 核心函数

**`buildUrl()`** — 构建真实广告 URL（V7 关键区别）：

```javascript
// V7: 直接构建 v1/link.php 广告 URL
this.url = protocol + "//" + this.config.syndication_host +
    "/v1/link.php?cat=" + this.config.cat +
    "&idzone=" + this.config.idzone +
    "&type=8&p=" + encodeURIComponent(referrer) +
    "&sub=" + this.config.sub +
    "&block=1&el=" + this.config.el +
    "&tags=" + this.config.tags +
    "&scr_info=" + encodeURIComponent(btoa(type + "|" + name + "|" + ver)) +
    "&gdpr=" + gdpr +
    "&cookieconsent=" + this.config.cookieconsent +
    "&cb=" + Math.floor(Math.random() * 1e9);
// 生成示例: https://s.pemsrv.com/v1/link.php?idzone=5866630&type=8&p=...&cb=123456789
```

**`shouldShow()`** — 频率检查：

```javascript
shouldShow: function(e) {
    if (!popMagic.config.capping_enabled) { /* 无频率限制 */ }
    if (popMagic.open_count >= popMagic.config.frequency_count) return false;
    var i = popMagic.getCountFromCookie();
    var n = popMagic.getLastOpenedTimeFromCookie();
    // 检查触发次数和时间戳
    popMagic.open_count = i;
    return !(i >= popMagic.config.frequency_count);
}
```

**`setAsOpened()`** — 记录已触发：

```javascript
setAsOpened: function(e) {
    // 触发 creativeDisplayed 事件
    var i = new CustomEvent("creativeDisplayed-" + popMagic.config.idzone, {detail: t});
    document.dispatchEvent(i);
    // 更新 Cookie
    var n = 0 !== popMagic.open_count ? popMagic.open_count + 1 : popMagic.getCountFromCookie() + 1;
    var r = Math.floor(Date.now() / 1e3);
    popMagic.config.cookieconsent && popMagic.setCookie(popMagic.cookie_name, n + ";" + r, popMagic.config.frequency_period);
}
```

#### 弹窗方法

| 方法 | 适用场景 | 行为 |
|------|---------|------|
| `default` | 非 Chrome 浏览器 / 移动端 | 新标签页打开原链接，当前页跳转广告 |
| `chromeTab` | Chrome | 模拟 Ctrl+点击打开原链接，当前页打开广告 |
| `popup` | popup_force 或 popup_fallback | 弹窗打开原链接，延迟后跳转广告 |

选择逻辑：

```javascript
getPopMethod: function(e) {
    if (popMagic.config.popup_force || popMagic.config.popup_fallback && e.isChrome && e.version >= 68 && !e.isMobile)
        return popMagic.methods.popup;
    if (e.isMobile) return popMagic.methods.default;
    if (e.isChrome) return popMagic.methods.chromeTab;
    return popMagic.methods.default;
}
```

#### 触发方式 (trigger_method)

| 值 | 含义 | 绑定元素 |
|----|------|---------|
| 1 | 任意点击 | `document` |
| 2 | 特定 class | 配置的 `trigger_class` 元素 |
| 3 | 点击链接 | 所有 `<a>` 标签 |
| 4 | 非特定 class | `document`（排除特定 class） |
| 5 | 非链接 | `document`（排除 `<a>` 标签） |

#### Cookie 格式

```
名称: zone-cap-5866630
值:   {触发次数};{Unix时间戳}
TTL:  frequency_period 分钟
示例: 1;1710000000
```

---

## 二、下载弹窗广告 (Download Popunder)

### 文件结构

| 文件 | 作用 |
|------|------|
| `src/consts.ts` | 配置：开关、Zone ID |
| `src/components/DownloadPopunder.astro` | Astro 组件：直接重定向方式 |
| `src/components/DownloadAllImages.astro` | 触发入口：下载按钮带 trigger class |
| `src/pages/posts/[...slug].astro` | 渲染入口：博文详情页引入组件 |

### 配置参数

```typescript
// src/consts.ts
export const DOWNLOAD_POPUNDER_ENABLED = true;           // 全局开关
export const DOWNLOAD_POPUNDER_IDZONE = "5914280";       // 广告位 ID
```

### 实现方式

**DownloadPopunder.astro** — 直接重定向方式，每次点击下载按钮时在新标签页打开广告 URL：

```astro
---
import { DOWNLOAD_POPUNDER_ENABLED, DOWNLOAD_POPUNDER_IDZONE } from "@consts";

if (!DOWNLOAD_POPUNDER_ENABLED) {
  return;
}

const dlIdzone = DOWNLOAD_POPUNDER_IDZONE;
---

<script is:inline define:vars={{ dlIdzone }}>
(function() {
    function openPopunderAd() {
        var adUrl = "https://s.pemsrv.com/v1/link.php?cat=&idzone="
            + dlIdzone + "&type=8&p="
            + encodeURIComponent(window.location.href)
            + "&cb=" + Math.floor(Math.random() * 1e9);
        window.open(adUrl, "_blank");
    }

    function init() {
        var buttons = document.querySelectorAll(".download-pop-trigger");
        for (var i = 0; i < buttons.length; i++) {
            buttons[i].addEventListener("click", function() {
                openPopunderAd();
            });
        }
    }

    document.addEventListener("DOMContentLoaded", init);
    document.addEventListener("astro:page-load", init);
})();
</script>
```

### 广告 URL 参数说明

```
https://s.pemsrv.com/v1/link.php?cat=&idzone=5914280&type=8&p={来源页面}&cb={随机数}
```

| 参数 | 是否必须 | 说明 |
|------|---------|------|
| `idzone` | ✅ 必须 | 广告位 ID，决定收益归属 |
| `type` | ✅ 必须 | 广告类型，8=popunder 重定向 |
| `cat` | 可选 | 广告分类 |
| `p` | 可选 | 来源页面 URL，帮助广告商统计 |
| `cb` | 可选 | 随机数，防止浏览器缓存，确保每次点击算独立请求 |

### 与全站弹窗广告的区别

| 对比项 | 全站弹窗 | 下载弹窗 |
|-------|---------|---------|
| 实现方式 | popMagic V7 内联代码 | 直接重定向 URL |
| 触发方式 | 页面任意点击自动触发 | 仅下载按钮触发 |
| 频率控制 | Cookie 控制（本地） | 广告商控制（服务端） |
| idzone | 5866630 | 5914280 |
| new_tab | `false`（当前页跳转） | `true`（新标签页打开） |
| 渲染位置 | BaseLayout（全站） | 博文详情页 |

### 触发入口

**DownloadAllImages.astro** — 下载按钮带 `download-pop-trigger` 类名：

```astro
<button id="download-all-images"
  class="download-pop-trigger inline-flex items-center gap-1.5 px-4 py-1.5 ...">
  下载 ZIP
</button>
```

弹窗脚本通过 `querySelectorAll(".download-pop-trigger")` 监听该按钮的点击事件，点击时调用 `window.open()` 打开广告。

---

## 三、文章内嵌广告 (In-Article Native Ad)

### 文件结构

| 文件 | 作用 |
|------|------|
| `src/components/InArticleAd.astro` | 组件：广告容器 + 插入逻辑 + 样式 |
| `src/pages/posts/[...slug].astro` | 渲染入口：博文详情页引入 |

### 广告参数

| 参数 | 值 |
|------|------|
| Zone ID | 5910390 |
| 广告商 JS | `https://a.magsrv.com/ad-provider.js` |
| 广告类型 | Native Widget |
| ins class | `eas6a97888e2` |

### 插入逻辑

广告初始状态为 `display: none`，通过 JS 动态插入到文章内容中第2张图之前。

### 样式控制

```css
.in-article-ad-visible {
    display: block;
    width: 100%;
    max-width: 300px;
    height: auto;
    max-height: 100px;
    overflow: hidden;
    margin: 20px auto;
    text-align: center;
    background: transparent;
}
```

### 维护要点

- 无开关控制，始终显示（仅爬虫通过 BaseLayout 层面排除）
- Zone ID `5910390` 硬编码在组件中，更换时需修改 `InArticleAd.astro`

---

## 四、首页信息流广告 (AdWidget)

### 文件结构

| 文件 | 作用 |
|------|------|
| `src/components/AdWidget.astro` | 单个广告组件 |

### 广告参数

| 参数 | 值 |
|------|------|
| Zone ID | 5863494 |
| 广告商 JS | `https://a.magsrv.com/ad-provider.js` |
| ins class | `eas6a97888e20` |
| 尺寸 | 352×300 px |

### 渲染位置

目前未直接使用，被 AdWidgetTriple 替代。但代码保留可用。

---

## 五、首页伪装广告 (AdWidgetTriple)

### 文件结构

| 文件 | 作用 |
|------|------|
| `src/components/AdWidgetTriple.astro` | 三联广告组件（广告+博文封面伪装层） |
| `src/pages/index.astro` | 首页第1页引入 |
| `src/pages/page/[page].astro` | 首页分页引入 |

### 广告参数

| 参数 | 值 |
|------|------|
| Zone ID | 5863494 |
| 广告商 JS | `https://a.magsrv.com/ad-provider.js` |
| ins class | `eas6a97888e20` |
| 数量 | 3个广告并排 |

### 伪装逻辑

核心设计是**双层结构**：

```
┌─────────────────────────┐
│  广告层 (ad-layer, z-0)  │  ← ExoClick 广告，底层
│  内容层 (content-layer)   │  ← 博文封面伪装，顶层，pointer-events: none
└─────────────────────────┘
```

- **广告层**：ExoClick 原生广告，负责实际广告展示和点击计费
- **内容层**：随机选取博文封面、标题、标签覆盖在广告上方，使广告外观与普通博文卡片一致
- **pointer-events: none**：内容层不拦截点击，用户点击时实际触发底层广告

---

## 六、底部悬浮广告 (Bottom Ad)

### 文件结构

| 文件 | 作用 |
|------|------|
| `src/components/BottomAd.astro` | 组件：广告容器 + 显示逻辑 + 样式 |
| `src/layouts/BaseLayout.astro` | 渲染入口：全局布局中引入 |

### 广告参数

| 参数 | 值 |
|------|------|
| Zone ID | 5863710 |
| 广告商 JS | `https://a.magsrv.com/ad-provider.js` |
| ins class | `eas6a97888e17` |
| data-block-ad-types | `92,45`（限制广告类型） |

### 渲染位置

**BaseLayout.astro** — 全站底部，仅对非爬虫用户渲染：

```astro
{!isBot && <BottomAd />}
```

### 显示逻辑

- 与年龄验证弹窗有联动逻辑，避免同时显示
- 延迟1秒显示广告

---

## 广告商公共资源

### ExoClick/pemsrv 广告加载器

所有 ExoClick 广告共用同一个广告加载脚本：

```html
<script async type="application/javascript" src="https://a.magsrv.com/ad-provider.js"></script>
```

### ins 标签 class 命名规则

ExoClick 生成的 `ins` 标签 class 名与 Zone ID 关联：

| Zone ID | ins class | 用途 |
|---------|-----------|------|
| 5866630 | — | 弹窗广告（不使用 ins 标签） |
| 5910390 | `eas6a97888e2` | 文章内嵌广告 |
| 5863494 | `eas6a97888e20` | 首页信息流广告 |
| 5863710 | `eas6a97888e17` | 底部悬浮广告 |

---

## 弹窗广告技术要点

### 为什么使用 V7 而非 V8

官方提供的 pp.md 中是 V8 版本 popMagic 代码，但实际部署中出现了严重问题：

| 对比项 | V7（当前使用） | V8（官方代码） |
|--------|---------------|---------------|
| `buildUrl()` | 直接构建真实广告URL：`s.pemsrv.com/v1/link.php?idzone=...` | 构建脚本URL：`s.pemsrv.com/popunder1000.js?type=inline&...` |
| 依赖外部脚本 | `only_inline: true` 时不依赖 | 必须加载外部脚本才能生成正确广告URL |
| 外部脚本不可用时 | 仍然正常工作 | 弹窗打开脚本URL本身 → 404 |

### 为什么设置 only_inline: true

- `only_inline: false` 时，`loadHosted()` 会从 `a.pemsrv.com/popunder1000.js` 加载外部脚本
- 外部脚本实际是 V8 版本，会覆盖内联的 V7 代码
- V8 的 `buildUrl()` 生成错误的脚本URL而非广告URL，导致 404
- 设置 `only_inline: true` 阻止加载外部脚本，V7 内联代码独立运行

### 为什么设置 tcf_enabled: false

- `tcf_enabled: true` 要求网页有 `__tcfapi` 接口（GDPR 同意框架）
- 网站没有实现该框架，导致 `popMagic.browser` 为 null，后续代码报错
- 设置 `tcf_enabled: false` 跳过 TCF 检查

---

## 维护速查

### 更换广告位 ID

1. 修改 `src/consts.ts` 中的 `POPUNDER_IDZONE` 或 `DOWNLOAD_POPUNDER_IDZONE`（弹窗广告）
2. 修改对应 `.astro` 组件中硬编码的 `data-zoneid` 和 `ins class`（其他广告）
3. 同步更新 CSS 中的类名选择器（如 `.eas6a97888e2` → 新 class 名）
4. 重新构建部署

### 添加新广告开关

1. 在 `src/consts.ts` 添加 `export const XXX_ENABLED = true;`
2. 在对应 `.astro` 组件中 import 并条件渲染
3. 在 `功能开关说明.md` 中记录

### 临时禁用所有广告

在 `BaseLayout.astro` 中移除或注释广告组件引用即可影响全站弹窗和底部广告。文章内嵌和首页信息流广告需在各自页面单独处理。

### 调试广告

- 清除 Cookie 可重置弹窗广告频率限制
- 浏览器开发者工具 → Application → Cookies → 删除 `zone-cap_*`
- 弹窗广告脚本在控制台无日志输出，需通过断点调试
