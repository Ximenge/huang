# 广告门控下载方案

## 方案概述

在用户点击下载按钮时，先在新标签页打开广告重定向页面，再执行下载操作。采用直接重定向方式，每次点击都会触发广告，频率由广告商控制。

## 实现状态

✅ 已实现。当前方案使用直接重定向 URL 方式。

## 核心设计

### 触发机制

```
用户点击「下载 ZIP」按钮
  ↓
按钮带有 class="download-pop-trigger"
  ↓
脚本监听该 class 的点击事件
  ↓
window.open() 在新标签页打开广告重定向 URL
  ↓
同时执行下载逻辑
```

### 广告重定向 URL

```
https://s.pemsrv.com/v1/link.php?cat=&idzone=5914280&type=8&p={当前页面URL}&cb={随机数}
```

| 参数 | 是否必须 | 说明 |
|------|---------|------|
| `idzone` | ✅ 必须 | 广告位 ID，决定收益归属 |
| `type` | ✅ 必须 | 广告类型，8=popunder 重定向 |
| `cat` | 可选 | 广告分类 |
| `p` | 可选 | 来源页面 URL，帮助广告商统计 |
| `cb` | 可选 | 随机数，防止浏览器缓存 |

### 文件结构

| 文件 | 作用 |
|------|------|
| `src/consts.ts` | 开关和 idzone 配置 |
| `src/components/DownloadPopunder.astro` | 弹窗广告组件（直接重定向） |
| `src/components/DownloadAllImages.astro` | 下载按钮（带 trigger class） |
| `src/pages/posts/[...slug].astro` | 博文详情页（引入弹窗组件） |

## 开关控制

```typescript
// src/consts.ts
export const DOWNLOAD_POPUNDER_ENABLED = true;           // true=开启, false=关闭
export const DOWNLOAD_POPUNDER_IDZONE = "5914280";       // 广告位 ID
```

## 方案演变历史

### V1：popMagic V8 外部脚本（已废弃）
- 使用官方 pp.md 提供的 V8 代码
- `buildUrl()` 生成脚本 URL 而非广告 URL → 404 错误

### V2：popMagic V7 内联代码 + only_inline: true（已废弃）
- 改用 V7 内联代码解决 404 问题
- Cookie 频率控制 + 广告商服务端频率限制 → 两边限制叠加，只弹一次
- 代码复杂（~300行），但功能简单

### V3：直接重定向 URL（当前方案）
- 每次点击直接 `window.open()` 打开广告重定向 URL
- 频率完全由广告商控制
- 代码简洁（~20行），无外部依赖
- 使用新的 idzone `5914280`（popunder 重定向类型）

## 注意事项

1. 弹窗广告和下载操作**同时执行**，不需要等待广告加载完成
2. 广告在新标签页打开，不影响当前页面
3. 频率由广告商服务端控制，我们无法调整
4. 如果 `DOWNLOAD_POPUNDER_ENABLED = false`，下载按钮仍然正常工作，只是不弹广告
5. Chrome 可能拦截 `window.open()` 弹窗，用户需允许弹窗
