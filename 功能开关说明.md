# 功能开关说明

本文档记录项目中所有可通过手动配置控制的功能，包括开关、参数调整和使用方式。

---

## 功能总览

| 功能 | 开关变量 | 配置文件 | 默认状态 |
|------|---------|---------|---------|
| 全站弹窗广告 (Popunder) | `POPUNDER_ENABLED` | `src/consts.ts` | 关闭 |
| 下载弹窗广告 (Download Popunder) | `DOWNLOAD_POPUNDER_ENABLED` | `src/consts.ts` | 开启 |
| 博文置顶排序 (Pin Order) | `PIN_ORDER_ENABLED` | `src/consts.ts` | 开启 |

---

## 一、全站弹窗广告 (Popunder)

### 功能说明

用户在页面任意位置点击时触发弹窗广告，广告在当前页或新标签页打开。受频率控制，同一用户在设定周期内最多触发指定次数。

### 配置位置

**文件**: `src/consts.ts`

```typescript
export const POPUNDER_ENABLED = false;           // 开关：true=开启, false=关闭
export const POPUNDER_IDZONE = "5866630";        // ExoClick 广告位 ID
export const POPUNDER_FREQUENCY_PERIOD = 60;      // 频率限制周期（分钟）
export const POPUNDER_FREQUENCY_COUNT = 1;        // 周期内最多触发次数
export const POPUNDER_TRIGGER_METHOD = 1;         // 触发方式：1=任意点击
```

### 如何控制

| 操作 | 修改方式 |
|------|---------|
| 开启弹窗广告 | `POPUNDER_ENABLED = true` |
| 关闭弹窗广告 | `POPUNDER_ENABLED = false` |
| 每次点击都触发 | `POPUNDER_FREQUENCY_PERIOD = 0` 且 `POPUNDER_FREQUENCY_COUNT = 9999` |
| 每30分钟触发1次 | `POPUNDER_FREQUENCY_PERIOD = 30`，`POPUNDER_FREQUENCY_COUNT = 1` |
| 每1小时触发2次 | `POPUNDER_FREQUENCY_PERIOD = 60`，`POPUNDER_FREQUENCY_COUNT = 2` |
| 更换广告位 | 修改 `POPUNDER_IDZONE` 为新的 ExoClick zone ID |

### 代码逻辑

1. **组件**: `src/components/ExoClickPopunder.astro`
   - 读取 `consts.ts` 中的配置
   - 如果 `POPUNDER_ENABLED = false`，组件直接 `return`，不输出任何 HTML
   - 如果开启，输出内联 `<script>` 标签，通过 `define:vars` 注入配置参数
   - 使用 **V7 版本 popMagic 内联代码** + `only_inline: true` 模式

2. **核心脚本逻辑**（内联在组件中）：
   - V7 版本 popMagic 自执行函数，页面加载后自动初始化
   - `buildUrl()`: 构建真实广告链接 `s.pemsrv.com/v1/link.php?idzone=...&type=8&p=...`
   - 使用 Cookie (`zone-cap-{idzone}`) 记录触发次数和时间戳
   - `shouldShow()`: 检查 Cookie 中的触发次数是否小于 `frequency_count`
   - `setAsOpened()`: 触发后更新 Cookie，设置 TTL 为 `frequency_period` 分钟
   - 根据浏览器类型选择弹窗方式：
     - Chrome: `chromeTab` 方法（模拟 Ctrl+点击）
     - 其他浏览器: `default` 方法（当前页跳转广告，新标签页打开原链接）

3. **渲染位置**: `src/layouts/BaseLayout.astro`
   ```astro
   {!isBot && POPUNDER_ENABLED && <ExoClickPopunder />}
   ```
   - 仅对非爬虫用户渲染
   - 受 `POPUNDER_ENABLED` 开关控制

### 频率控制机制

```
Cookie 名: zone-cap-5866630
Cookie 值: {触发次数};{时间戳}
Cookie TTL: frequency_period 分钟

触发流程:
1. 用户点击页面
2. shouldShow() 读取 Cookie
3. 如果 Cookie 不存在 → 允许触发
4. 如果 Cookie 存在且触发次数 < frequency_count → 允许触发
5. 如果 Cookie 存在且触发次数 >= frequency_count → 不触发
6. 触发后 setAsOpened() 更新 Cookie
```

---

## 二、下载弹窗广告 (Download Popunder)

### 功能说明

用户在博文页面点击「下载 ZIP」按钮时，在新标签页打开广告重定向页面。采用直接重定向方式，每次点击都会触发弹窗，频率由广告商控制。

### 配置位置

**文件**: `src/consts.ts`

```typescript
export const DOWNLOAD_POPUNDER_ENABLED = true;           // 开关：true=开启, false=关闭
export const DOWNLOAD_POPUNDER_IDZONE = "5914280";       // ExoClick 广告位 ID
```

### 如何控制

| 操作 | 修改方式 |
|------|---------|
| 开启下载弹窗 | `DOWNLOAD_POPUNDER_ENABLED = true` |
| 关闭下载弹窗 | `DOWNLOAD_POPUNDER_ENABLED = false` |
| 更换广告位 | 修改 `DOWNLOAD_POPUNDER_IDZONE` |

### 代码逻辑

1. **组件**: `src/components/DownloadPopunder.astro`
   - 读取 `consts.ts` 中的配置
   - 如果 `DOWNLOAD_POPUNDER_ENABLED = false`，组件直接 `return`
   - 如果开启，输出内联 `<script>` 标签
   - 监听 `.download-pop-trigger` 按钮的点击事件
   - 点击时通过 `window.open()` 在新标签页打开广告重定向 URL：
     ```
     https://s.pemsrv.com/v1/link.php?cat=&idzone={IDZONE}&type=8&p={当前页面URL}&cb={随机数}
     ```
   - `p` 参数：告诉广告商点击来源页面（非必须，有助于统计）
   - `cb` 参数：防止浏览器缓存，确保每次点击都算独立请求（非必须，有助于计数准确）

2. **触发方式**: `src/components/DownloadAllImages.astro`
   - 下载按钮带有 `class="download-pop-trigger ..."`
   - 弹窗脚本自动监听该 class 的点击事件

3. **渲染位置**: `src/pages/posts/[...slug].astro`
   ```astro
   {DOWNLOAD_POPUNDER_ENABLED && <DownloadPopunder />}
   ```
   - 仅在博文详情页渲染

### 与全站弹窗广告的区别

| 对比项 | 全站弹窗 | 下载弹窗 |
|-------|---------|---------|
| 实现方式 | popMagic V7 内联代码 | 直接重定向 URL |
| 触发方式 | 页面任意点击自动触发 | 仅下载按钮触发 |
| 频率控制 | Cookie 控制（本地） | 广告商控制（服务端） |
| idzone | 5866630 | 5914280 |
| 渲染位置 | BaseLayout（全站） | 博文详情页 |

---

## 三、博文置顶排序 (Pin Order)

### 功能说明

允许手动控制博文在首页的展示顺序。开启后，设置了 `pinOrder` 的博文优先展示（数字越小越靠前），未设置的博文按发布日期降序排列。

### 配置位置

**全局开关**: `src/consts.ts`

```typescript
export const PIN_ORDER_ENABLED = true;   // 开关：true=开启, false=关闭
```

**单篇博文配置**: 每篇博文的 Markdown frontmatter

```yaml
---
title: "博文标题"
pubDate: 2024-01-01
pinOrder: 1          # 可选，数字越小越靠前
---
```

### 如何控制

| 操作 | 修改方式 |
|------|---------|
| 全局开启置顶 | `PIN_ORDER_ENABLED = true` |
| 全局关闭置顶 | `PIN_ORDER_ENABLED = false`（所有博文恢复按日期排序） |
| 置顶某篇博文 | 在博文 frontmatter 中添加 `pinOrder: 1` |
| 取消置顶 | 删除博文 frontmatter 中的 `pinOrder` 字段 |
| 调整置顶顺序 | 修改 `pinOrder` 的值，数字越小越靠前 |

### 代码逻辑

1. **Schema**: `src/content/config.ts`
   ```typescript
   pinOrder: z.number().optional(),   // 可选数字字段
   ```

2. **排序函数**: `src/utils/sortPosts.ts`
   - 第一步：所有博文按发布日期降序排列
   - 如果全局开关关闭，直接返回日期排序结果
   - 第二步：分离置顶和非置顶博文
   - 第三步：置顶博文按 pinOrder 升序排列
   - 第四步：置顶博文在前，非置顶博文在后

3. **使用位置**:
   - `src/pages/index.astro` — 首页第1页
   - `src/pages/page/[page].astro` — 首页第2页及之后

---

## 修改配置后的操作

所有配置修改都在 `src/consts.ts` 或博文 frontmatter 中完成，修改后需要：

```bash
# 1. 重新构建
npm run build

# 2. 部署到 Cloudflare Pages 生产环境
npx wrangler pages deploy dist --project-name astro-melody-starter --branch main
```

---

## 文件索引

| 文件路径 | 涉及功能 |
|---------|---------|
| `src/consts.ts` | 所有功能的开关和参数配置 |
| `src/components/ExoClickPopunder.astro` | 全站弹窗广告组件（V7 内联代码） |
| `src/components/DownloadPopunder.astro` | 下载弹窗广告组件（直接重定向） |
| `src/components/DownloadAllImages.astro` | 下载按钮（带 download-pop-trigger 类名） |
| `src/utils/sortPosts.ts` | 博文排序逻辑 |
| `src/content/config.ts` | pinOrder 字段定义 |
| `src/pages/pinned.astro` | 置顶博文管理页 |
| `src/pages/index.astro` | 首页（使用排序函数） |
| `src/pages/page/[page].astro` | 分页页（使用排序函数） |
| `src/pages/posts/[...slug].astro` | 博文详情页（下载弹窗） |
| `src/layouts/BaseLayout.astro` | 全局布局（全站弹窗） |
