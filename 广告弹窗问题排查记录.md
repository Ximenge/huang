# 广告弹窗问题排查记录

> 日期：2026-05-03 ~ 2026-05-05
> 问题：点击首页前3个博文格子后页面跳转到广告页面
> 根因：评论区 XSS 注入攻击

---

## 一、问题起因

2026年5月3日部署新博文后，用户发现点击首页前3个大格子对应的博文链接时，页面会短暂加载博文内容，然后自动跳转到广告页面（`//ja.cat/tu/`）。按浏览器后退键无法返回原页面，因为跳转是在当前窗口直接修改了 `window.location.href`。

**受影响的3个博文**：
1. `axuexue-_No_046_bilanhangxian_nengdai_qipao_96P6V_5_82GB01`
2. `mashudawang-No016-yujiadianshangdeyuejie-75P1V-838MB`
3. `mashudawang-No017-rongrongtianxin-40P-463MB`

**正常博文**（对比）：
- `mashudawang-No018-jingxidiaoluocaidan-35P-395MB` — 无跳转

---

## 二、排查过程

### 第一阶段：怀疑广告组件问题

**思路**：网站集成了 ExoClick 广告（AdWidgetTriple、InlineAdReplacer、InArticleAd、BottomAd、DownloadPopunder），怀疑是广告配置问题。

**操作**：
1. 给所有广告 `<ins>` 标签添加 `data-block-ad-types="92,45"` 阻止 Fullpage Interstitial (FPI) 和 Popunder 广告格式
2. 部署后测试 → **问题依然存在**

### 第二阶段：对比版本差异

**思路**：问题出现在5月3日部署之后，5月2日的部署没有问题，对比两次部署的代码差异。

**操作**：
1. 使用 `git diff` 对比 `backup-2026-05-03-pre-vast` 和当前版本
2. 发现主要变更是新增了 Video.js + VAST 视频广告集成
3. 但广告组件（AdWidgetTriple、InlineAdReplacer）代码本身没有变化
4. VAST 广告仅作用于视频页面，与博文页面无关

**结论**：代码变更不是直接原因

### 第三阶段：逐个关闭广告组件排查

**思路**：通过代码级关闭（注释掉组件）来定位是哪个广告组件导致问题。

**操作与结果**：

| 关闭的组件 | 结果 |
|-----------|------|
| 关闭 AdWidgetTriple + InlineAdReplacer | ✅ 问题消失 |
| 只开 AdWidgetTriple | ✅ 无问题 |
| 只开 InlineAdReplacer | ✅ 无问题 |
| 两个都开 | ❌ 问题出现 |

**初步结论**：两个组件使用相同的 ad zone ID (5863494)，可能触发 FPI 广告

### 第四阶段：全关广告仍有问题

**思路**：如果广告组件是原因，全关应该彻底解决。

**操作**：
1. 关闭所有广告组件（AdWidgetTriple、InlineAdReplacer、InArticleAd、BottomAd、DownloadPopunder）
2. 关闭 ExoClick 站点验证 meta 标签和 Delegate-CH meta 标签
3. 部署测试 → **问题依然存在！**

**关键发现**：广告组件不是根因！

### 第五阶段：检查服务器端注入

**思路**：如果客户端代码没有广告脚本，那可能是服务器端或 CDN 层注入的。

**操作**：
1. 用 `curl` / `Invoke-WebRequest` 检查线上博文页面的 HTML
2. 对比问题页面和正常页面的 `<script>` 标签
3. 发现两个页面的脚本完全一致，没有额外的广告脚本
4. 唯一的外部脚本：Google Analytics、Cloudflare Beacon、Astro 页面脚本

**结论**：服务器端没有注入广告

### 第六阶段：检查 Cloudflare 层

**思路**：Cloudflare 可能在边缘层注入了内容。

**操作**：
1. 检查 Cloudflare Workers — 无自定义 Worker
2. 检查 Cloudflare Functions — 仅有项目自带的 API 函数
3. 检查 Transform Rules — 无规则
4. 检查 Page Shield — 未启用

**结论**：Cloudflare 层没有注入

### 第七阶段：定位评论区 XSS

**思路**：既然服务器端 HTML 没有广告脚本，但浏览器中仍然跳转，说明是客户端 JavaScript 动态执行的。排查所有使用 `insertAdjacentHTML` 和 `innerHTML` 的地方。

**关键发现**：
- `PostComments.astro` 的 `renderComment()` 函数直接拼接用户输入到 HTML
- `guestbook.astro` 的 `renderComment()` 函数同样直接拼接
- 两个地方都通过 `insertAdjacentHTML()` 插入 DOM

**验证**：关闭 PostComments 组件后部署 → **问题消失！**

### 第八阶段：提取恶意评论数据

通过 API 直接查询 KV 存储：

```
GET /api/post-comments?postId=axuexue-_No_046_bilanhangxian_nengdai_qipao_96P6V_5_82GB01
```

返回内容：

```json
{
  "comments": [{
    "id": "17778319008115oo7",
    "nickname": "ekisa",
    "content": "<img src=\"x\" onerror=\"window.location.href='//ja.cat/tu/';\">",
    "ip": "151.115.79.60",
    "timestamp": 1777831900811
  }]
}
```

3个受影响博文各有一条相同攻击者（IP: 151.115.79.60）的恶意评论。

---

## 三、根因分析

### XSS 攻击原理

```
攻击者 → POST /api/post-comments → content字段注入HTML → 存入KV（无转义）
                                                          ↓
用户访问博文页面 → 加载评论 → renderComment()直接拼接HTML → insertAdjacentHTML() → 浏览器解析执行
                                                          ↓
                                              <img src="x" onerror="恶意代码">
                                                          ↓
                                              图片加载失败 → 触发onerror → window.location.href跳转
```

### 漏洞代码

**PostComments.astro** (修复前)：

```javascript
function renderComment(comment) {
  return '<p>' + comment.content + '</p>'  // 直接拼接，未转义
}

// 通过 insertAdjacentHTML 插入 DOM
commentsList.insertAdjacentHTML('beforeend', renderComment(comment));
```

**post-comments.ts** (修复前)：

```typescript
const newComment = {
  content: body.content.substring(0, 500),  // 直接存储，未转义
};
```

### 攻击 Payload 解析

```html
<img src="x" onerror="window.location.href='//ja.cat/tu/';">
```

- `<img src="x">` — 加载一个不存在的图片
- `onerror="..."` — 图片加载失败时执行的 JavaScript
- `window.location.href='//ja.cat/tu/'` — 在当前窗口跳转到广告页面

### 为什么只影响前3个博文？

攻击者特意选择了首页前3个大格子对应的博文进行注入，这样首页用户最容易点到，影响面最大。

---

## 四、修复方案

### 双重防护策略

#### 第一层：服务端防护（API 层）

在 `post-comments.ts` 和 `comments.ts` 中添加：

**1. 恶意内容检测** — 直接拒绝提交：

```typescript
function containsMaliciousContent(str: string): boolean {
  const lower = str.toLowerCase();
  const patterns = [
    /<\s*script/i,
    /<\s*img[^>]*onerror/i,
    /<\s*iframe/i,
    /<\s*object/i,
    /<\s*embed/i,
    /<\s*svg[^>]*onload/i,
    /<\s*body[^>]*onload/i,
    /<\s*input[^>]*onfocus/i,
    /<\s*details[^>]*on toggle/i,
    /<\s*marquee[^>]*onstart/i,
    /<\s*video[^>]*onerror/i,
    /<\s*audio[^>]*onerror/i,
    /javascript\s*:/i,
    /on\w+\s*=/i,
    /data\s*:\s*text\/html/i,
    /vbscript\s*:/i,
    /expression\s*\(/i,
    /url\s*\(\s*javascript/i,
    /document\.(cookie|location|write|domain)/i,
    /window\.(location|open|eval)/i,
    /eval\s*\(/i,
    /setTimeout\s*\(\s*['"]/i,
    /setInterval\s*\(\s*['"]/i,
    /new\s+Function\s*\(/i,
  ];
  return patterns.some(pattern => pattern.test(lower));
}
```

**2. HTML 实体转义** — 存储前转义，即使绕过检测也无法执行：

```typescript
function sanitizeHtml(str: string): string {
  return str
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;')
    .replace(/'/g, '&#x27;');
}
```

#### 第二层：客户端防护（渲染层）

在 `PostComments.astro` 和 `guestbook.astro` 中添加：

```javascript
function escapeHtml(str) {
  var div = document.createElement('div');
  div.appendChild(document.createTextNode(str));
  return div.innerHTML;
}

function renderComment(comment) {
  var safeNickname = escapeHtml(comment.nickname);
  var safeContent = escapeHtml(comment.content);
  return '<p>' + safeContent + '</p>' + '<span>' + safeNickname + '</span>';
}
```

---

## 五、经验总结

### 排查思路总结

1. **先排除自身代码**：逐个关闭可疑组件，定位问题范围
2. **对比版本差异**：用 git diff 找出变更点
3. **检查服务器端**：用 curl 检查线上 HTML 是否被注入
4. **检查 CDN/代理层**：Cloudflare Workers、Transform Rules 等
5. **检查客户端动态内容**：所有使用 `innerHTML` / `insertAdjacentHTML` 的地方
6. **直接查数据源**：通过 API 查询 KV 存储中的实际数据

### 关键教训

1. **永远不要信任用户输入**：所有用户提交的内容（评论、留言）在存储前必须转义，在渲染时也必须转义
2. **双重防护**：服务端转义 + 客户端转义，缺一不可
3. **`insertAdjacentHTML` 是 XSS 高危操作**：任何通过它插入的 HTML 都必须确保用户输入已转义
4. **排查问题时不要过早下结论**：最初怀疑广告组件，但全关后问题仍在，说明需要更全面的排查
5. **直接查数据源最可靠**：通过 API 查询 KV 比通过 wrangler CLI 更可靠（CLI 可能有缓存或权限问题）

### 常见 XSS 攻击向量

| 攻击向量 | 示例 |
|---------|------|
| `<script>` 标签 | `<script>alert(1)</script>` |
| `<img>` onerror | `<img src=x onerror="恶意代码">` |
| `<svg>` onload | `<svg onload="恶意代码">` |
| `<body>` onload | `<body onload="恶意代码">` |
| `<iframe>` 嵌入 | `<iframe src="恶意URL">` |
| 事件处理器 | `<div onmouseover="恶意代码">` |
| javascript: 伪协议 | `<a href="javascript:恶意代码">` |
| CSS expression | `style="width:expression(恶意代码)"` |

### 后续待办

- [ ] 清除 KV 中残留的3条恶意评论（等 Cloudflare KV 写入限制重置后执行）
- [ ] 考虑给评论系统添加管理员审核功能
- [ ] 考虑添加 Content Security Policy (CSP) 头部作为额外防护层
- [ ] 考虑对攻击者 IP (151.115.79.60) 进行封禁
